# TIL | 2026-08-09
## 공부한 내용 | git / github 


### - reset, revert, restore : 되돌리기 3종 세트
예시 |

```
* a3f21c1 (HEAD -> main) 로그인 기능 추가
* b7d82e4 회원가입 기능 추가
* c1a45f9 프로젝트 시작
```
#### - reset : 커밋 / 스테이징을 되돌림
- 스테이징한 파일이나 커밋을 이전 상태로 되돌릴수 있다 = git add 한것을 다시 내리거나 이전 커밋으로 되돌아간다.
```
git reset HEAD~1 (한단계전 커밋으로 돌아간다.)
            v
* b7d82e4 (HEAD -> main) 회원가입 기능 추가
* c1a45f9 프로젝트 시작

```
처럼 최근 커밋이 현재 브랜치의 기록에서 사라진것처럼 보인다.

#### | git reset의 핵심 옵션 3가지

#### " --soft" : 가장 안전한 방법 : 커밋만 되돌림

--> 커밋 기록만 지우고 코드를 고친 변경내용은 그대로 남겨둔다.

--> '아, 방금 커밋메세지 잘못적었네..! 커밋만 취소하고 다시 적어야지!

#### " --mixed" : 기본값 : 커밋 + add 되돌림

--> 커밋도 취소하고, 방금 작업한 파일들을 다시 풀어서 수정하고 싶을때 

--> '커밋 기록도 지우고, 스테이징 (git add)했던것도 다시 내려야지!'
 
#### " --hard" : 가장 위험하지만 강력한 방법 : 커밋 + add + 파일 변경사항 되돌림

--> 현재 커밋 이후의 커밋기록과 내가 방금 수정한 에디터 내용까지 싹다 날려버린다.

--> '커밋도 취소하고, 내가 수정했던 내용도 전부 없애야지!'


#### - revert : 파일의 변경사항을 되돌림 (이미 GitHub에 push했거나 다른 사람들과 공유한 커밋을 취소할 경우 많이 사용됨)

- -이미 만든 커밋의 작업을 취소하는 새로운 커밋을 만들어준다. = 기존 커밋은 남겨두고, 그 작업을 취소하는 새로운 커밋을 만든다.

> ** 이미 GitHub에 올린 커밋을 다른사람들과 공유한 상황에 취소하는거라면 revert가 안전함

```
git revert HEAD
           v
* e8c31a2 (HEAD -> main) Revert "로그인 기능 추가"
* a3f21c1 로그인 기능 추가
* b7d82e4 회원가입 기능 추가
* c1a45f9 프로젝트 시작
```
처럼 기존 커밋은 그대로 있고, Revert라는 새로운 커밋이 추가되있다.

#### - restore : 파일의 수정사항을 되돌림
- 아직 커밋하지 않은 파일 수정사항을 버릴때 많이 사용한다.


```
git restore README.md
           v
* a3f21c1 (HEAD -> main) 로그인 기능 추가
* b7d82e4 회원가입 기능 추가
* c1a45f9 프로젝트 시작
```
처럼 커밋을 만드는 명령어가 아니라 파일 내용을 되돌리는 명령어이기때문에 커밋 로그는 변화가없다.


### - git merge : 현재 브랜치에 다른 브랜치를 합치는 명령

- 따로 작업하던 내용을 하나의 브랜치에 합치기 위해서 병합을 한다. 

- 여러명이 main브랜치에서 동시에 작업을하면 서로의 작업이 부딪힐수있기때문이다.

#### | merge 병합의 종류 

#### 1. Fast-forward merge
- 'git merge'를 했을때, 브랜치가 갈라지지 않았다면 일어나는 병합 방식
- 직선 구조
```
minji@gimminjiui-MacBookPro cart-practice % git merge feature/login
Updating bd1ed59..1c21d6c
Fast-forward
 info.md | 1 +
 1 file changed, 1 insertion(+)
 
* 1c21d6c (HEAD -> feature/login) feat:로그인 기능 추가
* bd1ed59 (origin/main, main, feature/cart-add) docs:내용 추가 및 수정
          v
* 1c21d6c (HEAD -> main, feature/login) feat:로그인 기능 추가
```
fast-forward 방식으로 브랜치가 병합되었음을 확인할 수 있다.

#### 2. 3-Way merge
- 'git merge'를 했을때, 브랜치가 서로 갈라져 있다면 일어나는 병합 방식
```
minji@gimminjiui-MacBookPro cart-practice % git log --oneline --graph --all --decorate
*   e0cbd7f (HEAD -> 3merge-main) merge: 3merge-text 병합
|\  
| * 853df89 (3merge-test) feat:주문 기능 추가
* | 67884d9 feat:결제기능 추가
|/  
* bd1ed59 (origin/main, feature/cart-add) docs:내용 추가 및 수정
```
- '* bd1ed59' 이 두 브랜치가 갈라지기 전의 공통 커밋이다. (출발점)
- 3merge-main에서 '* bd1ed59'인 커밋을 기준으로 결제 기능을 추가하여 '* 67884d9'커밋이 생성됨
- 반대로 3merge-test에서는 '* bd1ed59'를 기준으로 주문 기능을 추가하고 '* 853df89'커밋이 생성되었다.
- 결과적으로 두 브랜치가 갈라졌다.
- '* e0cbd7f'이 갈라진 두 브랜치의 작업을 하나로 합친 merge 커밋이다.


#### merge의 주요 옵션 |' --no-ff' : Fast-forward가 가능한 상황에서도 일부러 merge commit을 만드는 옵션이다.

=>Fast-forward로 합칠 수 있지만 "이 브랜치의 작업이 여기서 합쳐졌다"는 메모를 남김



#### 0. merge conflict 병합 충돌
- 두 브랜치가 같은 파일의 같은 부분을 서로 다르게 수정했을때 발생

- Git이 "Gitdl 둘 중 어떤 내용을 선택해야 할지 모르겠는데?"라고 판단하여 충돌이 발샌한다.
          
```bash
CONFLICT (content): Merge conflict in info.md
Automatic merge failed; fix conflicts and then commit the result.
```
이렇게 알려주고 파일을 열어보면 Git이 충돌난 부분을 표시해준다.
```
<<<<<<< HEAD
결제 가능
=======
배송 가능
>>>>>>> 3merge-test
```
- '<<<<<<<< HEAD' : 현재 브랜치의 내용
- '=======' : 두 내용을 구분해줌
- '>>>>>>> 3merge-test' : 병합하려는 브랜치의 내용

이런식으로 충돌이 난 부분을 표시해주고있다. 직접 수정을 하고, 후에는 기호 표시들은 반드기 삭제해줄것.
그리고 git add 로 Git에게 해결했다고 알려주고 커밋한다.




### - git remote : github 와의 연결

### - git stash : 아직 커밋하고 싶지 않은 현재 작업을 임시로 치워두는 기능

### - git rebase : 히스토리 정리

### - gitignore : Git이 추적하지 않아도 되는 파일이나 폴더를 지정하는 설정파일(명령어가 아니다.)
