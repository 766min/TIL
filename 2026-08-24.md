c

### 모듈 : 자주 쓰는 변수, 함수, 클래스들을 모아둔 독립적인 파이썬 파일(.py)
  - 코드가 깔뜸해져서 가독성이 향상된다.
  - 문제가 생기면 전체 코드가 아닌 해당 모듈 파일만 수정하면 된다.

### 패키지 : 여러 모듈들을 종류별로 깔끔하게 정리해 둔 폴더
  - 패키지 안에는 또 다른 서브 폴더가 존재할수있고 이 경로를 찾아갈때는 . 온점 기호를 사용한다.
  [ 상대 경로 표기법 ]
    - 단일 점 : 현재 자기가 속한 폴더
    - 더블 점 : 나를 감싸고 있는 바로 위 상위 폴더
  
### | 모듈을 가져오기 위한 키워드

- import : 모듈 전체를 통째로 가져오기
  - < 예시 > `import math`
  - 사용할 때 `math.sqrt(16)` 이렇게 math를 붙여주어 출처를 밝혀주어야한다.

- from ... import : 모듈 안에서 필요한 기능이 있는 함수 또는 클래스만 가져오기
  - < 예시 > `from math import sqrt`
  - 사용할 때 `sqrt(16)` 이렇게 출처를 생략하고 함수이름만 있어도 바로 쓸 수 있다.

- as : 별명 붙여주기
  - < 예시 > `import math as m`
  - `m.sqrt(16)`이렇게 math모듈을 별칭m으로 부르겠다 선언해서 바로 사용가능하다.

### | 파이선 표준 라이브러리 
- 파이썬을 설치하면 기본으로 저장되있는 라이브러리

| 대표적인 라이브러리
  - sys : 파이썬 인터프리터의 시스템 경로를 확인하거나 제어할 때 사용
    - 실무에서 내가 만든 파이썬파일이나 공유 받은 모듈을 불러올려고 import를 사용했는데, 파이썬이 그런거없다면서 에러내는 경우가있다.
      - `sys.path`를 써주면 파일 경로를 못 찾을 때 강제로 길을 터줄 때쓴다.
      - `sys.exit()`는 에러나 비상 상황일때 프로그램을 즉시 멈출려할때 쓴다.
      - `sys.argv`는 터미널에서 명령어를 전달받아 처리할 때 쓴다.

  - random : 무작위 숫자를 뽑거나 순서를 섞을 때 사용
    - `random.randint()` : 괄호안의 숫자 범위에서 랜덤으로 하나를 뽑아준다.
    - `random.choice()` : 리스트같은 시퀀스 안에서 하나 골라준다.
    - `random.shuffle()` : 시퀀스요소를 뒤죽박죽 섞어준다.

  - datetime : 현재 년, 월, 일, 시, 분, 초 같은 시간 데이터를 조회하고 원하는 모양으로 변경할 때 사용
    - `datetime.datetime.now()` : 현재 시간을 알려준다.
    - `strftime()` : 연도-월-일 시:분:초 이렇게 포멧팅해준다.
      - "%Y" : 년 "%m" : 월 "%d" : 일 "%H" : 시 "%M" : 분 "%S" : 초

#### - 의존성 : 내 프로그램 혼자서는 안되고 다른 외부 도구에 의해 의지해있는 관계

#### - PyPI : 파이썬 오픈소스 패키지가있는곳 (pandas같은도구들 가져올 수 있다.)

#### - uv.lock : 내컴퓨터에서는 되는데 왜 니한테선 안되지의 버전 싸움을 차단해주는 보안 잠금 장치

#### - uv add : 터미널에 uv add 패키지명 입력하면 PyPI에서 알아서 찾아서 내 프로젝트에 넣어주고 pyproject.toml에도 알아서 적어준다.
  - 이때 자동으로 uv lock 파일이 생성된다.

#### - uv sync : 방이 엉망일 떄 uv lock을 참고해서 누락된걸 채워 원래의 개발 환경으로 되돌려준다.

#### - uv init : 새로운 방을 처음 시작할때 입력하는 명령어
[ uv init 입력했을때 자동으로 생기는 대표적인 파일 ]
  - pyproject.toml : 프로젝트의 이름, 파이썬 버전 등을 기록해두는 메인설정파일(명세서)

  - README.md : 프로젝트에 대한 설명서
  - .python-version : 파이썬 버전 고정해둔 작은 쪽지 파일
  - .venv : 가상환경


## | 예외처리

#### 구문 오류 - Syntax Error
  - 파이썬 문법 규칙을 어겼을 때 발생
  - 프로그램이 실행되기도 전에 인터프리터가 미리 발견해서 아예 실행을 막는다.
  - < 예시 > 괄호 누락, 오타, 들여쓰기 실수

#### 런타임 예외 - Exception
  - 문법은 맞지만, 프로그램이 실행되는 도중에 상황에 맞지 않는 짓이 있을 때 발생
    - IndexError : 존재하지 않는 리스트 인덱스를 조회했을 때
    - ValueError : 자료형 타입은 맞지만 값이 맞지 않을 때
    - TypeError : 서로 더할 수 없는 다른 데이터 타입끼리의 연산을 시도할 떄
    - ZeroDivisonError : 숫자를 9으로 나눌려할 때

#### | try - except - else - finally 방어 시스템
- 'try: ' : 에러 생길 것 같은 코드를 여기 격리된 방 안에서 먼저 실행시켜보는 공간

- 'except: ' : 예상대로 try방안에서 에러가 발생하면 프로그램이 죽지않고 대응을 준비해놓은 공간

- 'else: ' : 에러 없이 try방에서 완벽하게 성공했을 때 추가로 실행되는 성공 전용 공간

- 'finally: ' : 성공하든 실패하든 상관없이 무조건 맨 마지막에 실행되는 마무리 공간

#### | raise키워드 : 의도적으로 에러 내기
- 파이썬입장에서는 문법적으로 오류가 아니지만 개발자가 만든 기획 의도에서 어긋나는 경우에 raise키워드를 사용하여 에러를 강제로 발생시킨다.
  - `raise ValueError("에러 메시지~~")`
  - 보통 조건문 안에서 규칙이 어긋났을때 사용한다. try블록 안에서도 가능

## | 상속과 캡슐화

#### - 상속 : 튼튼하게 지어둔 부모클래스를 그대로 복사해 와서 자식클래스에서 사용하기

#### - 메서드 오버라이딩 : 상속받은 메서드 이름은 그대로 쓰되, 자식클래스에서의 목적에 맞게 내용을 새롭게 재정의(덮어쓰기)해서ㅓ 사용하기

#### - super() 함수 : 부모의 생성자 호출하며 확장하기
  - 부모 클래스의 초기화 함수나 메서드(\_\_init\_\_)는 그대로 가져오고 재사용하고 싶을 때 사용한다.

#### - Getter : 프라이빗 변수를 안전하게 조회만 해주는 통로 함수
```python
def get_balance(self):
  return self..__balance
```
이렇게 숨겨진 값을 안전하게 반환 즉 보여주기만한다. 그리고 Getter는 return이 무조건 필수다. 숨겨진 값을 반환하는거기때문에.!

#### - Setter : 외부에서 그 프라이빗 변수를 수정하려고 할 때, 그 값이 올바른지 검사(유효성 검증)를 한뒤에만 값을 바꿔주는 통로
```python
def set_balance(self): 
  if amount >= 0:
    self.__balance = amount
    print(f"잔액이 {amount}원으로 변경되었습니다.")
  else:
    print("경고 : 잔액은 음수가 될 수 없습니다.")
```
Setter는 무조건 조건문이 필요하다.

[ 전체 흐름 코드 ]
```python
class ProtectedAccount:
  def __init__(self, owner, balance):
    self.owner = owner
    self.__balance = balance

  def get_balance(self):
    return self,__balance

  def set_balance(self, amount):
    if amount >= 0:
      self.__balance = amount
      print(f"성공 : 잔액이 {amount}원으로 변경되었습니다.")
    else:
      print("차단 : 잔액은 마이너스가 될 수 없습니다.")
```
[테스트 출력]
```python
acc = ProtecteAccount("홍길동", 3000)

# Getter를 이용해서 안전하게 잔액만 조회한다.
print(add.get_balance()) #결과 : 3000원 출력

# 직접 건드리려고 할때 - 정보 은닉 방어벽 작동
acc.__balance = -99999 #잔액은 바뀌지않는다.

# Setter를 이용하는데 비상식적인 값으로 수정을 시도할 때
acc.set_balance(-1000) # 출력 : "차단 : 잔액은 마이너스가 될 수 없습니다." 라며 방어한다.

# Setter를 통해 정상적인 값으로 수정을 시도할 때
acc.set_balance(5000) # 출력 : "성공 : 잔액이 5000원으로 변경되었습니다."
```



### | 데일리 퀘스트 | 

Q1. 다음 중 1이상 45 이하 범위에서 무작위 정수 하나를 반환하는 코드로 올바른 것은?

[ 보기 ]

a. random.randint(1, 45)

b. random.random(45)

c. random.sample(1, 45)

d. random.choice(45)

A. a. random.randint(1, 45) (정 답 ~)

-> random.randint는 지정된 범위 내에서 정수 난수를 반환하는 함수이다. b. random.random은 인자를 받지않고 0.0 ~ 0.1미만의 실수를 반환하므로 정수를 넣는것은 잘못된 사용이고 c. random.sample()과 random.choice()는 시퀀스를 첫 인자로 받아야 하므로 정수 하나만으로 사용이 불가능하다.

---

Q2. 여러 값을 하나의 변수에 튜플 형태로 묶어 담는 것을 '패킹'이라고 합니다. 반대로 하나의 자료구조에 담긴 값들을 여러개의 변수에 순서대로 나누어 담는 동작을 무엇이라고 하나요?

A. 언패킹 ( 정 답!!)
-> 시퀀스 자료구조의 요소들을 각각 독립된 변수에 순서대로 대입하여 분해하는 기술을 언패킹이라고하며, 이때 자료구조의 요소 개수와 변수의 개수가 일치해야 정상적으로 분해된다.

---

Q3. 리스트 numbers = [0, 1, 2, 3, 4, 5]가 있을 때, print(numbers[::2])의 실행 결과로 올바른 것은?

A. [0, 2, 4] (정답!)

-> 슬라이시으이 세 번째 자리는 증감폭을 의미하며 2를 지정했으니 처음부터 2칸 간격으로 요소를 추출하여 [0, 2, 4]가 반환된다.

---

Q4. 부모 클래스로부터 물려받은 메서드를 자식 클래스의 요구사항에 맞게 새롭게 재정의하여, 호출 시 부모의 원본 메서드 대신 실행되도록 하는 객체지향 기법을 무엇이라 하나요?

A. 메서드 오버라이딩 ( 정 답 )

-> 자식 클래스의 부모와 동일한 이름의 메서드를 다시 구현하면, 자식 객체에서 해당 메서드를 호출할 때 부모의 원본 대신 재정의된 메서드가 최우선으로 실행된다. 이를 메서드 오버라이딩이라 한다.

---

Q5. 다음 중 프로그램 실행 도중에 발생하는 '런타임 예외(Exception)'에 해당하는 것은?

[ 보기 ]

a. 함수 정의 끝에 콜론을 빠뜨린 경우
b. 코드 블록의 들여쓰기 칸 수가 일치하지 않는 경우
c. 실행 중에 0으로 나누기 연산을 시도한 경우
d. 괄호를 짝 맞추지 않고 여는 경우

A. c. 실행 중에 0으로 나누기 연산을 시도한 경우 ( 정답 ! )

-> 콜론누락, 들여쓰기 불일치, 괄호 미매칭은 모두 실행 전 단계에서 인터프리터가 걸러내는 구문 오류-Syntax Error이다. 반면 0으로 나누는 연산은 문법적으로 문제가 없으나 실행 중에 ZeroDivisionError라는 런타임 예외를 발생시킨다.


### | 회고
오늘 처음으로 데일리퀘스트 정답을 다 맞췄다. ㅎㅎ TIL에쓴 내용 이 그대로 데일리퀘스트에 나와서 더 잘 맞출수있었다. 귯..