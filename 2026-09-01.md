# TIL | 2026-09-01
## 공부한 내용 | 머신러닝 - 심층 신경망과 순환 신경망 구조

### | RNN

- 문제점 : 기억손실,  병목현상

### | 시퀀스-투-시퀀스

- Seq2Seq - 순서가 있는 입력을, 순서가 있는(길이가 다를 수도 있는)출력으로 바꾸는 문제를 푸는 구조
    - 즉, 단어들이 순서대로 나열된 리스트 하나를 통째로 읽고, 단어들이 순서대로 나열된 리스트(길이가 다를수도있음)를 새로 만들어낸다.

### | 인코더-디코더 아키텍쳐

- 입력을 이해하는 부분(인코더)과 출력을 만드는 부분(디코더)을 나눠서 연결한 구조
    - 순서 있는 입력 → 순서 있는 출력
- 인코더가 하는 일
    - 입력 문장을 한 단어씩 읽으면서, ‘지금까지 읽은 내용’을 계속 압축된 벡터(은닉상태)로 갱신한다.
    - 마지막 단어까지 다 읽으면 그 최종 은닉상태가 컨텍스트 벡터가 된다.
- 디코더가 하는 일
    - 인코더가 넘겨준 컨텍스트 벡터를 출발점(초기 은닉상태)으로 삼아서 출력 단어를 한 개씩 순서대로 생성한다.
    - 이때 매번 ‘직전에 내가 만든 단어’도 같이 참고하면서 다음 단어를 만든다.

### | LSTM(Long Short-Term Memory)

- 인코더와 디코더 내부에서 실제로 문장을 처리하는 엔진으로 흔히 LSTM을 사용한다. (일반 RNN대신)
- 왜냐면 RNN은 문장이 길어지면 기울기 소실 → 장기의존성 문제가 심하기때문이다.

LSTM의 핵심 - 기억을 두 종류로 나눈다. (값value)

- 은닉상태(h) - 단기 기억 (RNN과 비슷)
- 셀 상태(c) - LSTM에서 새로 추가된 장기 기억용 별도 통로

→ 셀 상태는 컨베이어 벨트다. 정보를 그대로 흘려보내면서, 필요한 부분만 게이트로 살짝 수정한다. 매번 정보를 완전히 새로 짓이겨 만드는 RNN과 달리, 정보가 멀리까지 손실 없이 잘 전달된다.

LSTM의 3개 게이트(계산식) : 문장의 단어 하나하나마다 매번 작동한다.

1. 망각 게이트 : 기존 기억 중 뭘 버릴지 결정
    1. 직전 은닉상태 + 지금 단어를 보고, 시그모이드(0~1)로 ‘얼마나 유지할지’의 비율을 계산
    2. f_t(망각게이트가 계산한 결과값) = [0.9, 0.1, 0.7, … ]같은 벡터(기억의 각 부분마다 유지 비율이 디름)
    3. 예를 들어 ‘그런데’라는 접속사가 나오면 → ‘앞 내용은 이제 덜 중요해질 수도 있으니 좀 지워도 돼’식으로 학습된다.
2. 입력 게이트 : 새 정보 중 뭘 추가할지 결정
    1. ‘이번 새 정보를 얼마나 반영할지’비율
        1. 얼마나를 구할때는 시그모이드로 새 정보를 몇%나 반영할지 비율정함
        2. 뭘 을 구할때는 탄젠트로 비율이아닌 실제 ‘새로 추가할 내용’ -1 에서 1로 표현
        3. 이 둘을 곱해서 진짜 추가할 양이 나온다.
3. 셀 상태 업데이트 : 새 정보 중 뭘 추가할지
    1. 기존 기억을 망각 게이트 비율만큼만 남긴것 + 새 정보를 입력 게이트 비율만큼만 더한것 = 새로운 셀 상태 완성
4. 출력 게이트 : 지금 당장 출력으로 내보낼 정보를 결정
    1. 방금 업데이트한 셀 상태 중 얼마나 밖으로 보여줄지의 비율 - 시그모이드
    2. 업데이트된 셀상태를(C_t) tanh로 -1 에서 1로 압축한 후, 그 비율(o_t)만큼 걸러서 은닉상태로 내보낸다.

| 예시

- ‘나는’ → [망각→입력→출력 게이트 다 거침] → 은닉상태1, 셀상태1

- ‘학교에’ → [망각→입력→출력 게이트 다 거침] → 은닉상태2, 셀상태2

- ‘간다’ → [망각→입력→출력 게이트 다 거침] → 은닉상태3, 셀상태3 ←이게 마지막이면 이 은닉상태3, 셀상태3만 특별히 ‘컨텍스트 백터’라고 부른다.

ㄴ> 핵심 포인트 - 왜 셀 상태가 잘 안지워지는지

- 셀 상태 업데이트 공식에서 볼수있듯이 곱셈만 있고, 활성화 함수(tanh)를 다시 씌우는 부분이 없다. (마지막에 은닉상태 뽑을때만 tanh)
- RNN은 매번 tanh로 정보를 ‘짓이겨서’ 새로 만들었지만, 셀 상태는 ‘곱하고 더하기’만 하면서 그대로 흘러간다.
- 그래서 기울기가 반복적으로 tanh미분값을 거치며 작아지는 문제가 훨씬 덜하다
- 이게 LSTM이 기억 손실을 완화하는 진짜 수학적 이유 !!!

| 인코더 LSTM : 읽기 전담 - 원문을 다 읽으면서, 뭘 기억하고 뭘 잊을지 판단 후 마지막 (hidden, cell) - 컨텍스트 벡터를 만든다.

1. 망각게이트(자기만의 가중치 W_f, b_f)
2. 입력게이트 (자기만의 가중치 W_i, b_i, W_C, b_c)
3. 출력게이트 (자기만의 가중치 W_o, b_o)

| 디코더 LSTM : 생성 전담 - 그 (hidden, cell)을 자기 시작점으로 받아서, 지금까지 만든 것중 뭘 기억하고 뭘 잊을지 판단 

1. 망각게이트(인코더와 다른 가중치 W_f, b_f)
2. 입력게이트(인코더와 다른 가중치 W_i, b_i, W_C, b_C)
3. 출력게이트(인코더와 다른 가중치 W_o, b_o)

-> 만약 게이트(가중치)를 공유한다면, 인코더용으로 최적화된 판단 기준이 디코더에도 그대로 강제되니까, 서로 다른 역할에 각자 맞게 특화되지못한다. 그래서 완전히 독립된 가중치 세트를 갖는다.

-> 게이트는 요리 레시피고 가중치는 그 레시피에 들어가는 실제 재료 배합 비율이다. 레시피를 공유한다는건 결국 똑같은 배합비율을 쓴다는 것. 

-> 인코더와 디코더는 레시피자체는 똑같은 LSTM구조를 쓰지만 배합비율(가중치)은 서로 다르게 독립적으로 학습한다.

#### | LSTM 한계와 다음 발전

- 이 구조(LSTM 기반 Seq2Seq)의 문제는, 여전히 ‘긴 문장을 벡터 하나(컨텍스트)에 압축’해야 한다는 병목이 있다.

‘병목현상’ : 넓은걸 좁은 통로로 억지로 통과시켜야해서 정보/흐름이 확 좁아지며 막히는 → 즉, 컨텍스트벡터는 크기가 항상 고정되있다(256차원) 근데 입력 문장의 길이는 고정이 아니다.

- 결국 문장이 아무리 길고 정보가 많아도 컨텍스트벡터라는걸 거쳐야 하니 정보가 눌려 담기다가 손실된다.

| 실제로 뭐가 손실되나 : 

- 50단어짜리의 문장에서 초반에 나온 ‘부산’같은 세부 정보는 256차원벡터라는 좁은 통로를 통과하면서 ‘대충 여행 관련 얘기였다.’정도로 뭉개지고, ‘부산’이라는 구체적 정보는 잘 안 남을수 있다. 
- ( 참고로 기울기 소실이랑도 연결된다. - 인코더 RNN이 문장을 순서대로 처리하면서, 마지막 은닉상태를 만들 때쯤엔 초반 정보의 영향력이 이미 희미해져있으니)

( LSTM은 기억을 못하는 문제는 완화됐지만 병목은 고치지 못했다.)

| 그래서 어텐션이 병목을 어떻게 없앴나 : 

- 기존(병목O) : 문장 전체 → [고정 크기 컨텍스트 벡터 1개] → 디코더

- 어텐션(병목 없음) : 문장 전체 → [단어마다 각각의 벡터를 다 유지] → 디코더가 필요할 때마다 다 참고

-> 즉, 어텐션은 요약본 한장(컨텍스트 벡터)으로 퉁치지 않고 ‘원문 전체를 그대로 펼쳐두고 필요할 때마다 꺼내보는’방식이라서 문장이 길어져도 정보가 억지로 눌려담기지 않는다.



### | 어텐션 메커니즘

도입 배경 | 

- 기존 LSTM Seq2Seqsms는 입력 문장 전체 -> 최종 컨텍스트 벡터 1개 -> 디코더인데 여기서 문제는 입력 문장이 아무리 길어도 결국 하나의 고정된 벡터에 다담아야했다. 그러니 초반의 중요한 정보가 손실될 수 있다.

- 나는 -> h1    학교에 -> h2    간다 -> h3 마지막 h3하나를 컨텍스트 벨터로 사용

|

- 어텐션은 컨텍스트 벡터를 하나만 만들지 않는다.
    - 각 단어를 읽었을 때의 은닉상태를 모두 보관한다.
    - '마지막 요약본 줄게'에서 '원문을 읽으면서 나온 정보들을 전부 가지고있어. 필요한 거 골라서 봐'로 할수있다.

- 나는 -> h1    학교에 -> h2    간다 -> h3에서 이 h1, h2, h3을 버리지않고 전부 가지고 있다. 즉, 각 단어를 읽었을 때의 은닉상태를 모두 보관한다.

| 그런데 디코더가 모든 단어를 똑같이 봐야 하나 ? NO

- 디코더는 자기가 지금 어떤 단어를 만들고 있는지에 따라서 필요한 정보를 다르게 봐야한다.

    - 에를 둘어 나는 -> h1    부산에 -> h2    갔다 -> h3 에서 디코더가 'I'를 만들 때는 '나는'이 중요할거고 'Busan'을 만들 때는 '부산에'가 중요해진다.
    - 이렇게 디코더가 매 순간 지금 내가 만들고 있는 단어와 관련해서 입력 문장의 어느 부분을 더 봐야할지를 계산한다.
    - 이게 어텐션의 핵심

Queary

Key

Value































### | 트렌스포머 아키텍쳐


### | 데일리 퀘스트 |

Q1. Seq2Seq 모델에서 인코더가 디코더에 전달하는 '컨텍스트 벡터'의 내부 구성 요소로 옳은 것은?

[ 보기 ]

a. LSTM 인코더의 마지막 시점 은닉 상태(h_T)와 셀 상태(c_T) 튜플 세트

b. 입력 단어 시퀀스의 텍스트 문자열 데이터 자체

c. 학습 과정에서 누적되 오차(Loss) 최적화 매개변수

d. 전체 어휘 사전에 등록된 토큰 인덱스들의 산술 평균값

A. a. LSTM 인코더의 마지막 시점 은닉상태와 셀 상태 튜플세트 ( 정 답 ~! )

-> LSTM은 단기 기억(은닉 상태)과 장기 기억(셀 상태)을 함께 유지하므로, 인코더가 압축한 컨텍스트 벡터도 이 두 상태를 튜플로 묶어 디코더에 전달한다. 

---

Q2. 디코더 학습 시, 이전 시점의 예측값 대신 실제 정답 단어를 다음 시점 입력으로 강제 주입하여 학습을 안정화하는 기법의 명칭을 쓰시오.

A. (뭐지 . .)

-> 교사 강요 ! 교사강요는 학습 초기 디코더의 잘못된 예측이 연쇄적으로 이어지는 것을 막고 수렴 속도를 높이기 위해 실제 정답을 다음 입력으로 주입하는 기법이다.

--- 

Q3. 어텐션 메커니즘에서 디코더의 현재 상태 벡터에 해당하며, 정보를 탐색하기 위한 '질문'역할을 하는 요소는 ?

[ 보기 ]

a. Key

b. Query

c. Value

d. Blas

A. b. Query ( 정 답 -! )

-> Query는 현재 시점에서 정보를 찾고자 하는 주체인 디코더의 상태 벡터이다.

---

Q4. Seq2Seq 모델과 비교했을 때, 트랜스포머(Transformer)가 채택한 설계 철학으로 옳은 것은?

[ 보기 ]

a. RNN의 양방향 구조를 강화하여 순차 처리 속도를 개선했다.

b. RNN 구조를 완전히 배제하고 어텐션만으로 병렬 연산 구조를 설계했다.

c. 고정된 크기의 컨텍스트 벡터 하나에 전체 문맥을 더 강하게 압축했다.

d. 문장을 한 단어씩 순서대로 처리하는 방식을 그대로 유지했다.

A. a. (오 답) 트렌스포머의 특징인 빠른것과 연결된것같아서 선택했다..

[ 정 답 ] - b. RNN구조를 완전히 배제하고 어텐션만으로 병렬 연산 구조를 설계했다.

-> 트렌스포머는 RNN의 순차 처리로 인한 속도 한계를 극복하기 위해 순환 구조를 없애고 셀프 어텐션 기반의 병렬 처리 구조를 채택했다.


--- 

Q5. 트렌스포머 디코더에서 학습 시 미래의 정답 단어를 미리 참조하지 못하도록 차단하는 기법의 이름은 ?

[ 보기 ]

a. 패딩 마스킹(Padding Masking)

b. 위치 인코딩(Positional Encodding)

c. 인과적 마스킹(Causal Masking)

d. 층 정규화(Layer Normailzation)

A. a. (오 답) .. 마스킹이라는건 알고있었지만 명확한 이름을 몰랐다.

[ 정 답 ] - c. 인과적 마스킹

-> 인과적 마스킹은 하삼각행렬 형태의 마스크를 적용해 미래 시점의 단어 정보를 -무한대로 만들어 소프트맥스 확률을 0으로 만들어 컨닝을 방지한다. 패딩 마스킹은 의미 없는 패딩 토큰을 무시하는 용도이다.