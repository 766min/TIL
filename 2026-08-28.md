# TIL | 2026-08-28
## 공부한 내용 | 머신러닝



#### - 로지스틱 회귀(Logistic Ragression) : 카테고리 분류

- 선형회귀의 재료로 만들어졌다. 

- 선형회귀와 똑같은 방정식 'y = wx + b`계산을 그대로하는데 여기서 끝나지 않고, 그 결과값을 '활성화 함수'라는걸로 한번더 통과시킨다. 이함수는 -무한대 부터 +무한대로 나오는 z날것의 값을 0과1사이로 압축시킨다.

- 로지스틱 회귀의 목적 : 예측시킬 문제 답이 숫자가 아닌 '이 메일이 스팸인가 정상인가'같은 대상을 딱 떨어지게 갈라놓는 분류가 유일한 목적이다.

- 컴퓨터는 이 정답값 y에 '스팸이다/ 아니다'를 넣었다고해서 그 글자를 바로 이해하기 어렵다. 그래서 선형회귀의 뼈대(wx+b)를 빌려와서 일단 날것의값 z를 뽑아낸다. 이 z는 로지스틱 회귀에서 마이너스 무한대부터 플러스 무한대까지 어떤 숫자들될수있다.

- 

- 

#### - 활성화 함수(Activation Function) : 

#### - 시그모이드

#### - 소프트맥스

#### - raw score / 로짓(Logits, z) : 

#### - 크로스 엔트로피(Cross Entropy) : 

#### - BCE / CCE / SCCE : 

#### - K-Fold 교차 검증(Cross Validation) :

#### - Shuffle : 

#### - 원핫 인코딩 (One-Hot_Encoding) : 

#### - 레이블 인코딩의 편향 : 

#### - 차원의 저주 / 더미 변수 함정 : 

#### - 데이터 누수 : 

























### | 데일리 퀘스트 | 

Q1. 선형 회귀와 로지스틱 회귀의 관계에 대한 설명으로 가장 올바른 것은 ?

[ 보기 ] 

a. 로지스틱 회귀는 선형 회귀의 출력값을 활성화 함수로 0과 1사이 확률로 변환하여 분류를 수행한다.

b. 로지스틱 회귀는 선형 회귀와 전혀 관련 없는 완전히 다른 계산 방식이다.

c. 로지스틱 회귀는 연속적이 숫자값을 예측하는 회귀 전용 알고리즘이다.

d. 로지스틱 회귀는 활성화 함수 없이 선형 회귀식만으로 분류를 수행한다.

A.

[ 내 오답 ] b.선택 이유 : 로지스틱 회귀는 회귀가 아닌 분류라고 생각하여 선형회귀와 다른 계산 방식이라고만 생각했다. 하지만 로지스틱회귀는 선형회귀의 뼈대(공식)를 그대로 가져와서 마지막에 0과1사이의 확률로 바꿔주는(시그모이드 또는 소프트맥스)만 엊은것이다.

[ 정답 ] a. 로지스틱 회귀는 선형 회귀로. raw score(z)를 계산한 뒤 활성화 함수를 통해 0~1사이 확률값으로 변환하여 분류 문제를 해결한다. 

--- 

Q2. 이진 분류와 다중 분류에서 사용하는 활성화 함수를 올바르게 짝지은 것은 ? 

[ 보기 ]

a. 이진 분류 : 소프트맥스 / 다중 분류 : 시그모이드

b. 이진 분류 : 시그모이드 / 다중 분류 : 소프트맥스

c. 이진 분류 : 시그모이드 / 다중 분류 : 시그모이드

d. 이진 분류 : 소프트맥스 / 다중 분류 : 소프트맥스

A. b. 이진 분류 : 시그모이드 / 다중 분류 : 소프트맥스 ( 정 답 -! )

-> 이진 크로스 엔트로피(BCE)는 시그모이드를 출력층 활성화 함수로 사용하고, 범주형/회소 범주형 크로스 엔트로피(CCE.SCCE)는 소프트맥스를 사용한다고 명시되어있다. 

---

Q3. 정답 레이블이 원-핫 인코딩이 아닌 정수형으로 되어있을때 메모리 효율을 위해 사용하는, 다중 분류용 손실 함수의 이름은 무엇일까요?

A. SCCE ( 정 답 -! )

-> SCCE(회소 범주형 크로스 엔트로피)는 정수형 레이블을 그대로 사용해 정답 인덱스의 확률값만 추출하여 연산하므로 원-핫 배열 생성없이 메모리를 절약할 수 있다.

---

Q4. K-Fold 교차 검증을 수행하기 전에 데이터를 반드시 Shuffle(섞기)해야 하는 이유로 가장 적절한 것은 ? 

[ 보기 ]

a. Shuffle을 하지 않으면 훈련 속도가 느려지기때문이다.

b. 데이터가 품종별로 순서대로 정렬되어 있을 경우, 특정 Fold에 한 클래스만 몰리는 불균형이 발생할 수 있기 때문이다.

c. Shuffle은 random_state값을 자동으로 고정시켜주는 기능이기 때문이다.

d. Shuffle을 해야만 테스트 세트와 훈련 세트의 크기가 같아지기 때문이다.

A. b. 데이터가 품종별로 순서대로 정렬되어 있을 경우, 특정 Fold에 한 클래스만 몰리는 불균형이 발생할 수 있기 때문이다.

-> 데이터가 품종같이 순서대로 정렬되어 있을 때 섞지 않고 Fold를 나누면 특정 조각에 한 품종만 100%몰리는 불균형 시험지가 만들어질수있다.

  - 여기서 c와 d가 헷갈렸었다. 그 이유는 
    - c는 random_state와 shuffle 코드를 같이 쓰다보니 개념이 연결되어서 헷갈였던것같다. 하지만. random_state는 셔플 기능 자체를 켜거나 자동으로 고정해주는게 아닌 셔플을 어떤규칙으로 무작위 섞을지 고정해주는 매개변수일뿐이다.
    - d는 테스트세트와 훈련세트의 크기를 같아지게하는 stratify와 헷갈려 비슷한 맥락으로 생각했다. 하지만 셔플은 데이터의 순서나 배치를 무작위로 섞는 역할이고 stratify는 비율을 똑같이 맞춰주는아이다. !

---

Q5. 범주형 데이터를 1, 2, 3처럼 단순 정수(레이블 인코딩)로 변환하지 않고, 각 범주에 독립된 열을 만들어 해당 열만 1로 표시하는 전처리 방식을 무엇이라고 할까요?

A. 원핫인코딩 One-Hot Encodding ( 정 답 -! )

-> 원핫 인코딩은 범주 간에 서열이나 크기 관계가 없음에도 정수가 변환할 때 생기는 수치적 편향성 문제를 해결하기 위해, 모든 범주에 독립된 열을 부여하고 해당하는 열만 1로 표시하는 전처리 기법이다.
