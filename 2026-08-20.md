# TIL | 2026-08-20
## 공부한 내용 | python 

### | 클래
 클래스 = 설계도

객체 = 설계도를 바탕으로 실제 만들어질 하나의 대상

속성 = 그 객체가 가지고 있는 정보

- \_\_init\_\_ : 객체가 만들어질때 자동으로 실행할 준비작업( = 객체를 만들때 초기값을 설정하고싶을때)을 만든 공간
  그래서 \_\_init\_\_는 객체를 처음만들때만 한번 실행이되고 이후에 클래스안의 다른함수 호출이 있어도 (여러번실행해도) \_\_init\_\_가 같이 실행되는게 아니다!!

- self : 현재 작동하고 있는 '객체 자기 자신'의 메모리 위치를 가리키는 참조변수 = 지금 사용할 객체

- 인스턴스

- 절차지향 

- 객체지향  


### | 클래스 상속과 캡슐화

- 상속 : 이미 만들어진 클래스 (부모 클래스)의 속성과 메서드를 그대로 물려받아 재사용한다.

- 매서드 오버라이딩 : 부모 클래스로부터 물려받은 매서드를 자식 클래스의 요구사항에 맞게 '새롭게 재정의'하는것 
  - 자식 객체에서 해당 메서드를 호출하면, 부모의 원본 메서드 대신 새롭게 덮어쓴 메서드가 최우선으로 실행된다.
```python

```
- super() : 부모 클래스의 메서드나 초기화 함수\_\_init\_\_를 호출할때 사용한다.

- 캡슐화 : 서로 관련 있는 데이터와 기능을 클래스 하나의 캡슐안에 모으는것

- 정보 은닉 : 외부에서 객체 내부의 민감한 데이터를 함부로 들여다보거나 수정하지 못하도록 차단하는것
  - 파이썬 프라이빗 변수 : 변수이름앞에 언더바 두개를 붙이면 외부에서 원래 이름으로 직접 접근할 수 없도록 이름이 바뀌는 '이름 변형 규칙'이 적용되는것이다.

- Getter 메서드

- Setter 메서드


### | 내장 함수

- isinstance() : 입력된 값이 정수형, 실수형, 문자열 등 자료형을 판별하여 참True 또는 거짓False로 반환해준다.
  - age = 20 print(isinstance(age, int)) 이렇게하면 age가 정수였으니 True가 출력된다.

- all() : 데이터목록의 모든 요소가 참True일때만 True를 반환 공백도 문자열이므로 True이다.
- any() : 데이터목록 중 단 하나라도 참True가 있으면 True를 반환
    - 빈데이터Empty : [ ]이렇게 빈 리스트일때 all은 목록에 어짜둥둥 False가 하나라도 없으니 True라고 판단하고 any는 목록에 True가 하나도 존재하지않으니 False로 판단한다.






[gpt코드 내가 풀이하기]
```python
class GameCharacter:
    def __init__(self, name, level):
        self.name = name
        self.level = level
        self.exp = 0

    def gain_exp(self, amount):
        self.exp += amount

        if self.exp >= 100:
            self.level += 1
            self.exp -= 100
            print(f"{self.name} 레벨 업!")

        print(f"{self.name}의 레벨: {self.level}, 경험치: {self.exp}")


character1 = GameCharacter("철수", 1)
character2 = GameCharacter("영희", 3)

character1.gain_exp(70)
character1.gain_exp(50)
character2.gain_exp(120)
```
1. `character1 = GameCharacter("철수", 1)` 과 `character2 - GameCharacter("영희", 3)`이 두 코드를 실행하고 직후의 객체 상태는 character1은 name = “철수” level = 1 exp = 0이고 character2 의 name = “영희” level = 3 exp = 0이다.

  -> 완벽쿙

2. `character1.gain_exp(70)` 을 실행할때 gain_exp(self, amount)에서 self는 GameCharacter(”철수”, 1)이되고   amount는 70이된다.
    
  -> 맞지만 표현을 좀더 정확하게 하자면 self는 character1이 가리키고 있는 GameCharacter객체를 가리킨다.
    
3. 첫번째  `gain_exp(70)` 코드가 실행된후에 character2의 값은 왜 변하지 않냐면 self가 가리키는 객체가 아니었기때문이다.

  -> 맞아유

4. 그다음으로 `character1.gain_exp(50)` 을 실행하면 경험치 exp가 70 + 50 = 120 이되는데 코드에서 더 보면 if문을 실행하니 self.exp ≥ 100: 이니깐 120 ≥ 100이므로 True! 이므로 다음 코드 실행해서 level은 2가되고   exp는 0이된다.  왜냐면 level은 초기값이 1이었는데 self.level = 1+ 1이되므로 2가되고 exp는 두번호출되서 120이 되엇고거기서 100을 빼니 20이된다.
    
  -> 맞아욤
    
5. `character2.gain_exp(120)` 을 실행하면 최종적으로 character1 은 name = “철수” level = 2 exp = 20 이고 charater2는 name = “영희” level = 4 exp = 20 이된다.
    
  -> 맞아유

6. `character1.gain_exp(70)` 과 `character2.gain_exp(120))` 이 두코드에서 self는 다른객체를 가리킨다. character1은 GameCharacter(”철수”, 1) 객체를 가리키고 character2 는 GameCharacter(”영희”, 3)객체를 가리킨다. 또 같은 gain_exp()메서드를 사용했지만 각 캐릭터의 경험치와 레벨이 따로 관리되는 이유는 실행했을때 self가 가리키는 객체가 다르기때문이고 그 객체를 가리키는 이름이 달랐기 때문이다! 그니까 똑같은 메서드를 실행하고있지만 self가 가리키는 객체 실행시킨 객체를 가리키는게 다르다!
    
  -> 맞지만 마지막에 객체를 가리키는 이름이 달랐다라는 표현보단 이름이달라서가 아닌 실제로 서로 다른 객체이기때문에가 핵심이다.


[gpt문제 내가 코드 짜기]

[ 문제 1 ]

[조건]

1. Player 클래스를 생성한다.
2. 객체 생성 시 이름과 직업을 입력받을 수 있도록 __init__을 작성한다.
예: player1 = Player("철수", "전사")
player2 = Player("영희", "마법사")
3. 객체가 생성될 때 체력(hp)은 100으로 시작한다.
4. name, job, hp를 객체의 속성으로 저장한다.
5. attack() 메서드를 만든다.
    - 자신의 이름과 직업을 사용하여 공격 메시지를 출력한다.
6. take_damage(damage) 메서드를 만든다.
    - 전달받은 damage만큼 자신의 체력을 감소시킨다.
    - 감소한 후의 체력을 출력한다.
7. 다음 코드를 실행한다.
player1.attack()
player1.take_damage(30)
player1.take_damage(20)
player2.take_damage(50)

8. 최종 출력 결과:
철수 전사가 공격합니다.
철수님의 남은 체력 : 70
철수님의 남은 체력 : 50
영희님의 남은 체력 : 50
```python
class Player:
    def __init__(self, name, job):
        self.hp = 100
        self.name = name #name을 객체의 속성으로 저장하는 작업
        self.job = job #job을 객체의 속성으로 저장하는 작업

    def attack(self):

        print(f"{self.name} {self.job}가 공격합니다.")

    def take_damage(self, damage):  
        #전달받은 damage만큼 자신의 체력을 감소시키기 
        self.hp -= damage     
        print(f"{self.name}님의 남은 체력 :  {self.hp}")




player1 = Player("철수", "전사")
player2 = Player("영희", "마법사")        

player1.attack()

player1.take_damage(30)
player1.take_damage(20)

player2.take_damage(50)
```

---

[ 문제 2 ]

[ 조건 ]

1. Thermostat 클래스를 생성한다.
2. 객체 생성 시 장소 이름과 현재 온도를 입력받을 수 있도록 __init__을 작성한다.
예: thermo1 = Thermostat("거실", 20)
thermo2 = Thermostat("방", 25)
3. place와 temp를 객체의 속성으로 저장한다.
4. increase() 메서드를 만든다.
    - 현재 온도를 1도 증가시킨다.
    - 변경된 온도를 출력한다.
5. decrease() 메서드를 만든다.
    - 현재 온도를 1도 감소시킨다.
    - 변경된 온도를 출력한다.
6. show_status() 메서드를 만든다.
    - 장소와 현재 온도를 출력한다.
7. 다음 코드를 실행한다.
thermo1.increase()
thermo1.increase()
thermo2.decrease()

thermo1.show_status()
thermo2.show_status()

8. 최종 출력 결과:
거실의 온도가 21도로 올라갔습니다.
거실의 온도가 22도로 올라갔습니다.
방의 온도가 24도로 내려갔습니다.
거실의 현재 온도 : 22도
방의 현재 온도 : 24도

```python
class Thermostat:
    def __init__(self, place, temp):
        self.place = place
        self.temp = temp

    def increase(self):
        self.temp += 1
        print(f"{self.place}의 온도가 {self.temp}도로 올라갔습니다.")

    def decrease(self):
        self.temp -= 1
        print(f"{self.place}의 온도가 {self.temp}도로 내려갔습니다.")

    def show_status(self):
        print(f"{self.place}의 현재 온도 : {self.temp}도")



thermo1 = Thermostat("거실", 20)
thermo2 = Thermostat("방", 25)        

thermo1.increase()
thermo1.increase()
thermo2.decrease()

thermo1.show_status()
thermo2.show_status()
```
코드를 짤 때 실수했던점은 increase메서드와 decrease메서드부분에서 매개변수로 (self, temp)를 넣어놨었다. 왜냐면 온도가 바뀌니까 값을 받아야한다고 생각해서 매개변수로 temp를 넣어놨다. 그래서 thermo1.increase() 이렇게 호출할때 계속 오류가 발생하고있었는데 temp가 들어갈자리가없다..? 당연히 매개변수로 temp를 넣어줘야된다고 생각하고있다보니 호출하는곳이 비어있는게 잘못된게아닌가 하며 지피티를 불신하고있었다.. 




### | Python 과제 | 8장 2강 : 상속과 캡슐화

문제 1. 클래스 상속과 스마트폰 기능의 확장

[ 조건 ]

1. Phone 클래스를 정의하고, 내부에 call 메서드를 생성하세요. call 메서드는 호출 시 화면에 "통화를 시작합니다"라는 문장을 출력하도록 구현하세요.
2. Phone 클래스를 상속받는 SmartPhone 클래스를 정의하세요.
3. SmartPhone 클래스 내부의 call 메서드를 오버라이딩하세요. 호출 시 화면에 "영상 통화를 시작합니다"라는 문장이 출력되도록 수정하세요.
4. SmartPhone 클래스 내부에 새로운 메서드인 search_web을 추가하세요. 호출 시 화면에 "인터넷 검색을 시작합니다"라는 문장이 출력되도록 구현하세요.
5. SmartPhone 클래스로 인스턴스를 생성한 후, call 메서드와 search_web 메서드를 각각 호출하여 결과를 확인하세요.

```python

```
---

문제 2. 정보 은닉과 안전한 스마트홈 도어락

[ 조건 ]

- DoorLock 클래스를 정의하고, 생성자에서 초기 비밀번호를 전달받아 저장하세요. 이때 비밀번호 변수명 앞에 언더바 두 개(__)를 붙여 프라이빗 변수로 설정하세요.
- 내부 프라이빗 비밀번호 값을 반환하는 get_password 메서드(Getter)를 정의하세요.
- 비밀번호를 변경할 수 있는 set_password 메서드(Setter)를 정의하세요.
- set_password 메서드 내에서 입력된 비밀번호가 4글자 이상인 경우에만 변경을 허용하고, "정상 변경되었습니다"라고 출력하세요. 4글자 미만인 경우에는 "경고: 비밀번호는 4자리 이상이어야 합니다"를 출력하고 기존 값을 유지하세요.
- 인스턴스를 생성한 후 프라이빗 변수에 직접 접근을 시도하여 접근이 제한되는지 확인하세요. 이어서 Setter를 통해 4자리 미만과 4자리 이상의 값을 각각 입력하여 유효성 검증 조건이 올바르게 작동하는지 출력 결과로 확인하세요.
- 코드를 실행하여 4자리 미만의 비밀번호 입력 시 경고 메시지가 출력되는지 확인하세요. 이어서 4자리 이상의 값을 입력했을 때 정상 변경 메시지가 출력되고, Getter를 통해 변경된 최종 비밀번호가 확인되는지 점검한 후 실행 화면을 캡처하여 작성된 코드와 함께 제출하세요

```python

```


---

문제 3. 부모 클래스를 호출하는 super()와 회원 관리 확장

[ 조건 ]

1. User 클래스를 정의하고, 생성자(__init__)에서 name을 매개변수로 받아 속성으로 설정하세요.
2. User 클래스를 상속받는 VIPUser 클래스를 정의하세요.
3. VIPUser 클래스의 생성자에서 name과 point 두 개의 매개변수를 받도록 정의하세요.
4. VIPUser 생성자 내부의 첫 줄에서 super() 함수를 명시적으로 호출하여 부모 클래스의 생성자가 name 속성을 먼저 초기화하도록 처리하세요.
5. 부모 생성자 호출 이후, VIPUser만의 고유 속성인 point 변수에 전달받은 값을 할당하세요.
6. VIPUser 인스턴스를 생성하고, 상속받은 name 속성과 새로 추가된 point 속성이 화면에 정상적으로 출력되는지 확인하는 코드를 작성하세요.














### | 데일리 퀘스트 풀이 |


#### Q1. 다음 코드를 실행했을 때 출력되는 결과로 올바른 것은 무엇인가요?

```python
scores = [45, 80, 55, 90, 30]
result = list(filter(lambda x: x >= 60, scores))
print(result)
```
A. [80, 90]  (정 답~)
-> 람다함수를 이용하고 filter메서드로 scores리스트의 내용을 거를건데 그 내용은 x >= 60 즉 60이상이어야 참True이므로통과다 통과된아이는 x에 담기고 그것들을 list()안에 넣어서 result 변수에 담아 출력하겠다.

---

#### Q2. 반복 가능한 객체(Iterable)의 모든 요소에 동일한 규칙(함수)을 일괄 적용하여 새로운 형태로 변환할 때 사용하는 함수의 이름은 무엇인가요?

A. 

[내 오답] sorted( )

[ 정답 ] map( )

-> map()은 리스트 등 반복 가능한 객체의 모든 요소에 동일한 함수를 일괄 적용해 새로운 값으로 변환해주는 함수이다.

---

#### Q3. 다음 중 파이썬 표준 라이브러리와 그 역할의 연결이 올바르지 않은 것은 무엇인가요?

[ 보기 ]

a. json - 파이썬 객체를 JSON 텍스트로 변환 및 복원
b. random - 무작위 수 추출 및 리스트 요소 무작위 선택
c. math - 현재 날짜 및 시각 기록과 프로그램 실행 시간 측정
d. od - 파일 디렉토리 탐색 및 경로 확인

A. math - 현재 날짜 및 시각 기록과 프로그램 실행 시간 측정

-> 현재 날짜 및 시각 기록, 실행 시간 측정은 datetime과 time라이브러리의 역할이다. math는 올림.내림.절대값.원주ㅜ율 등 정밀한 수학 연산을 처리하는 라이브러리이므로 해당 설명은 잘못 연결되어있다. 

---

#### Q4. 대용량 파일을 한 번에 메모리에 올리지 않고, 호출할 때마다 한 줄씩 순서대로 문자열을 읽어오는 파일 읽기 함수는 무엇인가요?

A.
[내 오답] 제너레이터

[ 정답 ] readline()

-> readline()은 호출할 때마다 다음 한 줄만 읽어오기 때문에 대용량 파일을 메모리 부담 없이 조금씩 처리할 수 있다. read()는 전체를 한 번에 문자열로 읽는거고 readlines()는 모든 줄을 리스트로 받는 함수이다. 

---

#### Q5. 1초가 걸리는 API 요청 10개를 동기식으로 순차 처리하면 총 10초가 소요됩니다. 동일한 요청을 asyncio.gather()를 이용해 비동기로 동시처리하면 총 소요 시간은 어떻게 되나요?

[보기]

a. 여전히 10초가 소요된다.
b. 가장 오래 걸리는 작업 하나의 시간만큼만 소요된다.
c. 모든 요청 시간의 합을 절반으로 나눈 시간이 소요된다.
d. 0초에 즉시 완료된다.

A. b.가장 오래 걸리는 작업 하나의 시간만큼만 소요된다. (정답)

-> 비동기 동시 처리는 각 요청의 대기 시간을 중첩하여 관리하므로, 전체 소요시간은 동기식처럼 개별 시간의 합니 아니라 가장 늦게 응답하는 작업 하나의 시간으로 단축된다. !!!

.번외.  API는 다른 프로그램한테 부탁하는 방법.. 내가 날씨를 알고싶은데 내 프로그램은 날씨를 모른다..그러면 날씨를 알고 있는 프로그램한테 서울 날씨 알려줘라고 부탁하는 방법이 API다 즉 프로그램끼리 대화하는 방법 ~ 
  그러면 저 문제에서 API요청 10개가 뭐냐면 내가 API에게 햄버거를 주문한다고치면 햄버거 한개 주문하고 받는데 1초가 걸린다. 이거를 동기식에서는 10개를 하나씩주문하니 총 10초가 걸리고 비동기식에서는 주문 10개를 한번에 넣는다 그러면 1초후 전부 완료가 된다. 암튼 그 햄버거하나 달라고 부탁하는 이 주문자체하나가 API 요청이다.