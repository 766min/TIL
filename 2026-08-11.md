# TIL | 2026-08-09
## 공부한 내용 | git / github 

### - git remote : GitHub와 연결하고 관리하는 역할
- 'git remote add origin 'GitHub주소''
  - 'origin'은 원격저장소의 별칭 
  - 'origin'이라는 이름으로 해당 GitHub저장소를 원격저장소로 등록한다.
- 'git remote -v' : 현재 어떤 GitHub저장소(원격 저장소)와 연결되있는지 확인할 수 있다.
- 'git remote remove origin' : 등록된 원격 저장소 연결정보를 삭제한다.
- 'git remote set-url origin '다른 GitHub주소'' : 원격 저장소 URL을 변경한다.

### - git stash : 아직 커밋하고 싶지 않은 현재 작업을 임시로 치워두는 기능

- main에서 작업하다가 갑자기 다른 브랜치의 버그를 고쳐야하는데 지금 main브랜치의 작업을 커밋하기는 싫다. 그때 !! 'git stash'로 현재 작업을 임시 보관하여 커밋하지않고 다른브랜치의 작업을 끝낼수있다.

- 'git stash list' : stash로 임시 저장한 목록을 확인할 수 있다.
```bash
minji@gimminjiui-MacBookPro TIL % git stash
Saved working directory and index state WIP on main: 480823c update TIL/merge end
minji@gimminjiui-MacBookPro TIL % git stash list
stash@{0}: WIP on main: 480823c update TIL/merge end
```
- 'git stash pop' : 가장 최근에 stash한 작업을 다시 가져오고, stash목록에서는 자동 삭제한다.
- 'git stash apply stash@{2}' : stash한 두번째 작업을 다시 가져오지만, stash목록에는 남겨둠


### - git rebase : 커밋 히스토리를 정리하는 명령어
- 포인트는! 내 브랜치의 커밋을 다른 브랜치의 최신 커밋 뒤로 다시 붙이는 것이다.
```bash
git switch feature
git rebase main
```
를 입력하면 feature브랜치의 커밋들이 main브랜치의 최신 커밋 뒤로 재배치된다.
-> 한 줄의 깔끔한 히스토리를 만들수있다.

> 주의점 | 이미 다른 사람과 공유한 브랜치에서는 rebase를 함부로 사용하면 안된다. 왜냐하면 rebase는 기존 커밋을 새로운 위치에 다시 만들기때문에 커밋해 시가 바뀌기때문이다.

### - gitignore : Git이 추적하지 않아도 되는 파일이나 폴더를 지정하는 설정파일(명령어가 아니다.)
- 프로젝트최상위에 ' .gitignore' 파일을 만들고 안에 GitHub에 올릴필요가 없거나( .env : 환경변수값, 비밀번호나 API키같은 민감한값이 들어있는파일) 올리면안되는 파일(자동으로만들어지는파일이나 설치된 패키지파일 등)을 넣어놓으면 굳이 Git이 추적하지 않는다

포인트 | ' .gitignore'은 이미 Git이 추적하고 있는 파일에는 적용되지않는다.!


#### - git rm : 파일을 삭제하면서 Git의 추적에서도 제거하는 명령어

- 'git rm test.txt' : 'text.txt'파일이 내컴퓨터에서도 삭제되고 동시에 Git에서도 추적이 해제된다.
- 'git rm --cached text.txt' : 'text.txt'파일은 놔두고 Git에서만 추적하지않게한다.
  - (' .gitignore'에 이미 추적중인 파일을 추가했을때 자주 사용됨.)

### - git push : 

git push -u origin main : 변경사항을 원격 저장소'origin'에 올린다. ' -u '옵션은 기본push 대상을 설정하며, 로컬'main' 브랜치과 원격 'origin/main'을 연결한다. 처음 push할때 설정해두면 그이후에는 git push만으로도 알아서 origin원격저장소에 올라간다.

'git clone'으로 시작했다면 이미 원격 저장소origin과 연결되어 있기 때문에 'git push'만 사용해도된다. 

> but, 다른 원격저장소에 push하고싶다면 'git push backup main' 이런식으로 'backup'이라는 원격저장소를 직접 지정해줘야한다.

'main' 브랜치가 아닌 다른 브랜치명로 push할 경우 'git push backup feature/login'으로 브랜치명도 변경해줘야한다.

### - git fetch: 원격저장소(GitHub)의 최신작업을 내 로컬 저장소로 가져오는 명령어

  - 'git pull'은 내 로컬 저장소로 가져와서 내 파일과 합쳤지만(merge까지) 'git fetch'는 가져오기만하고 합치지는 않는다.

  - 'git fetch origin' : origin원격저장소의 최신정보를 가져옴

### - git pull : 원격저장소(GitHub)의 최신 작업을 내 로컬 저장소로 가져와서 반영하는 명령어

  - 단순히 가져오기만 하는것이아님. ! GitHub에서 가져온파일과 내 컴퓨터의 현재 작업파일을 합쳐서 반영해주는것 ! (변경사항을 현재 브랜치에 합치는개념)
  
    - 'git fetch' + 'git merge' 의 상태라할수있다. 
  
  - 아직 커밋하지 않은 변경사항이 있는데 pull을 하면 충돌하거나 pull이 거부될수도 있다.

    - 커밋을 하거나 'git stash' -> 'git pull' -> 'git stash pop' 처럼 처리가능하다.


### - git mv : 파일명 변경 (사실 탐색기에서 변경해도된다.)
- 예시 : 파일명이 READEME.md로 오타가 나있었을때 README.md로 변경

변경 방법 : 
```bash 
 git mv READEME.md README.md
 ``` 
파일명을 변경한 후에는 스테이징 영역에 추가한다. :
```bash
git add . or git add README.md
```
현재 디렉터리의 변경된 파일들을 모두 스테이징 or README.md 파일만 스테이징 해준다. :
```bash
git commit -m "rename READEME.md to README.md"
```