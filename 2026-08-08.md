# TIL | 2026-08-08
## 공부한 내용 | git / github

### - github Repository (저장소) 연결 : 
#### | 내 컴퓨터에 프로젝트가 생성되있을때 GitHub에 연결

1. git --version : Git이 설치되었는지 확인한다. 
2. git config ~~ : 사용자 정보를 설정한다.
3. git init : 내 컴퓨터에 프로젝트가 생성되있을때 GitHub에 연결한다.
4. git add . : 변경사항을 스테이징한다.
5. git commit -m " ___ " : 변경사항을 커밋한다.
6. git remote add origin *깃허브 주소* : GitHub Repository를 원격 저장소로 연결한다.
7. git push -u origin main : 변경사항을 원격 저장소'origin'에 올린다. ' -u '옵션은 기본push 대상을 설정하며, 로컬'main' 브랜치과 원격 'origin/main'을 연결한다. 처음 push할때 설정해두면 그이후에는 git push만으로도 알아서 origin원격저장소에 올라간다.

#### | GitHub에 있는 Git 프로젝트를 내컴퓨터로 가져올때
1. git clone *깃허브 주소* : GitHub에 있는 Git 프로젝트를 내컴퓨터로 연결한다.
2. cd *폴더명* : 폴더 이동 명령 (파일명xx)

이후 프로젝트를 수정한 후에는 :

3. git add . : 스테이징한다.
4. git commit -m " ___ " : 변경사항을 커밋한다.
5. git push : 'git clone'을 하면 이미 원격 저장소origin과 연결되어 있기 때문에 'git push'만 사용해도된다. (다른 원격저장소에 push하고싶다면 'git push backup main' 이런식으로 지정해줘야한다.)

### - git --version : Git이 설치되어있는지 확인하는 명령어

### - git config : Git에서 사용할 사용자 정보나 설정을 지정하는 명령어

| git config 의 주요 옵션
- 옵션이 없다면 현재 Git저장소에만 적용한다. (기본값 = --local)
- --global : 이 컴퓨터의 모든 Git프로젝트에 적용한다.(보통 많이 사용)
- --local : 현재 Git 저장소에만 적용한다. (기본값)
- --system : 컴퓨터 전체의 모든 사용자가 사용하는 Git에 적용한다.

(범위가 커지는 순서 : local < global < system)

```
git config --global user.name "____"

git config --global user.email "____"
```
이름과 이메일을 프로젝트마다 다르게 설정할 필요는 거의 없으니 --global로 내 컴퓨터의 모든 프로젝트에 이름과 이메일은 한번에 설정한다.

### - git init : 현재 디렉토리 (폴더)를 Git 저장소로 만드는 명령

- 실행하면 해당 폴더 안에 .git 이라는
 폴더가 숨겨져 생성된다.
- ('pwd' : 현재 터미널이 어느 디렉토리(폴더)에 위치해 있는지 경로를 보여주는 명령어 )

### - git status : 현재 Git의 상태를 확인할 수 있는 명령어

#### => "파일이 수정됐나? add 됐나? commit은 됐나?" 를 확인할 수 있다.

- 아무것도 변경되있지않을때 : 'On branch main / nothing to commit, working tree clean' -> 현재 main 브랜치에 있고, 변경된 파일은 없다.
- 파일이 수정되있을때 : 'On branch main / Changes not staged for commit: / modified: README.md -> README.md 파일이 수정되어있지만 git add 하지않은상태다 (스테이징 영역에 올라가있지않다).
- git add README.md 한 후 : 'On branch main / Changes to be committed: / modified: README.md -> README.md가 스테이징 영역에 올라가서 (git add된 상태) 커밋이 가능하다.

### - git diff : 파일을 수정했을 때 뭐가 바꼈는지 확인할 수 있는 명령어 
#### => (!! git add 하지않은 변경사항만 확인할 수 있음!!)
- 에디터에서 내가 수정한 부분을 비교해서 보여준다. 

```
-1. git init : 내 컴퓨터에 프로젝트가 생성되있을때 GitHub에 연결한다.
-2. git add . : 변경사항을 스테이징한다.
-3. git commit -m " ___ " : 변경사항을 커밋한다.
+1. git --version : Git이 설치되었는지 확인한다. 
+2. git config : 사용자 정보를 설정한다.
```
- '-' 로 기존에 있던 내용과 
- '+' 로 새로 추가됐거나 수정된 내용을 비교해서 보여준다.

#### | git diff --staged : git add한 변경사항을 확인할 수 있는 명령어
#### | git diff HEAD : add한것과 add하지 않은것 모두 확인할 수 있는 명령어

### - git add : 변경된 파일들을 Stagging Area(스테이징 영역)에 추가하는 명령
#### => 커밋할 파일을 미리 준비해놓은 것.

- 'git add .' : . 은 현재 폴더를 가리킨다. 현재 폴더 안에서 변경된 파일들을 스테이징영역에 올린다.
- 특정 파일만 추가하고 싶다면 !! 'git add README.md' 같이 파일명을 지정하면된다.

### - git commit : Staging Area에 올라온 변경사항을 기록으로 저장하는 명령어
#### => 변경사항을 저장하고 나중에 어떤 작업을 했는지 확인할 수 있다.
- 'git commit -m "feat:로그인 기능 추가"' : 이번 커밋에는 로그인 기능을 추가했다 라는 메세지 (' -m'옵션으로)를 남김.

#### | commit의 메시지 타입
- feat : 새로운 기능 추가
- fix : 버그 수정
- docs : 문서 수정

#### git commit --amend : 마지막 커밋을 고쳐서 다시 만드는 명령

보통 만든 마지막!(HEAD가 가리키는)! 커밋에서 뭔가 빠졌거나 커밋 메세지를 잘못 적었을때 사용한다.
(방금 만든 마지막!(HEAD가 가리키는)! 커밋을 고치려는 상황이라면 커밋 삭제('reset','revert'등)말고 커밋 덮어씌우기(' --amend')를 추천한다.)

### - git branch : 하나의 프로젝트 작업 내용을 독립적으로 나누어 작업하는 명령
- 'git branch' : 현재 생성되있는 브랜치 목록을 확인한다. , 브랜치 위치도 확인한다.
```bash
minji@gimminjiui-MacBookPro cart-practice % 'git branch'
  feature/cart
'*' feature/cart-add
  main
```
이런식으로 현재 브랜치 목록과 HEAD위치에 '*'가 붙어있음을 확인할 수 있다.

- 'git branch feature/login' : 로그인 기능을 개발하기 위한 브랜치를 생성한다.
```bash
minji@gimminjiui-MacBookPro cart-practice % 'git branch feature/login'
minji@gimminjiui-MacBookPro cart-practice % git branch
  feature/cart
* feature/cart-add
  'feature/login'
  main
```
처럼 feature/login이라는 브랜치가 생성되어있다.('git branch' 입력으로 확인함.)

- 'git branch -d feature/login' : 'feature/login'이라는 브랜치를 삭제해라

[ 만약 -d 옵션으로 삭제가 안될때 강제 삭제는 -D 옵션이다. (** 현재 위치가 feature/login브랜치라면 삭제가 안되니 git switch ___로 HEAD위치를 다른브랜치로 먼저 변경해주어야한다. **) ]
```bash
minji@gimminjiui-MacBookPro cart-practice % git switch main
M       cart.md (cart.md파일의 내용이 수정되어있고 아직 커밋되지 않은 변경사항있음.)
Switched to branch 'main' (main브랜치로 이동함.)
Your branch is up to date with 'origin/main'. (내 main브랜치는 원격저장소 origin/main 브랜치와 동일한상태로, 서로 반영되지 않은 커밋은 없다.)
minji@gimminjiui-MacBookPro cart-practice % git branch
  feature/cart
  feature/cart-add
  feature/login
* main
minji@gimminjiui-MacBookPro cart-practice % git branch -d feature/login
Deleted branch feature/login (was bd1ed59).(feature/login 브랜치는 삭제했고, 삭제하기전에는 이 브랜치가 bd1ed59 라는 커밋을 가리키고있었다.)
```
처럼 main브랜치로 HEAD포인터를 이동한 후 feature/login 브랜치가 삭제되었음을 확인할수있다.


- 'git branch -m feature/order' : 'feature/order' 이름으로 브랜치 명을 변경해라.

### - git switch : 브랜치 위치 이동 명령 (HEAD 포인터 이동)

- 'git switch feature/login' : 'feature/login' 브랜치로 HEAD포인터를 이동한다.
```bash
minji@gimminjiui-MacBookPro cart-practice % 'git switch feature/login'
Switched to branch 'feature/login'
minji@gimminjiui-MacBookPro cart-practice % git branch
  feature/cart
  feature/cart-add
'* feature/login'
  main
```
처럼 feature/login 이라는 브랜치로 이동했다.(git branch 입력으로 바뀐것을 확인할 수 있다.)

- 'git switch -c feature/login' : 브랜치를 생성하고 해당 브랜치로 이동한다.
```bash
minji@gimminjiui-MacBookPro cart-practice % git switch -c feature/login
Switched to a new branch 'feature/login'
```
처럼 feature/login 브랜치를 생성하고 해당 브랜치로 동시에 이동했다.

### - git checkout : 브랜치 위치 이동(HEAD 포인터이동) 및 파일 복원 등에 사용되는 명령

- 'git checkout -b feature/login' : feature/login 이름으로 브랜치를 생성하고 이동한다.

- 'git checkout -- cart.md' : cart.md파일의 내용을 수정햇지만 그 내용을 아직 커밋하지않았을때 'git checkout -- cart.md'를 명령하면 내용수정하기 전 마지막 커밋내용으로 되돌아간다
> 하지만 !! 요즘엔 굳이 git checkout을 쓰지않고 기능을 나누어 만든 git switch를 사용해서 브랜치를 이동하고, git restore을 사용하여 파일복원을 한다.

### - git log : 커밋 기록 확인 명령
- git log : 모든 브랜치의 커밋 해시, 작성자, 날짜, 커밋 메시지 등을 자세하게 확인할 수 있다.
- 'git log --oneline' : 커밋 해시의 앞부분과 커밋 메시지를 한줄로 확인할 수 있다.
- 'git log --graph' : 커밋들의 연결관계를 그래프로 확인할 수 있다.(브랜치가 나뉘고 합쳐지는모습 등)
- 'git log --all' : 모든 브랜치의 커밋 기록을 확인할 수 있다.
- 'git log --decorate' : 커밋 옆에 브랜치나 태그가 어떤 커밋을 가리키고있는지 확인할 수 있다.(HEAD도 표시된다.)
> #### 조합해서 많이 사용한다 !!  git log --oneline --graph --all --decorate 
- #### 커밋을 한 줄로 간단하게 -> 커밋 연결 관게를 그래프로 -> 모든 브랜치의 커밋을 -> 브랜치/태그/HEAD 이름을 표시한다.
```bash
minji@gimminjiui-MacBookPro TIL % git log --oneline --graph --all --decorate
* c8cbe00 (HEAD -> main) update TIL/log end (내 로컬저장소인 컴퓨터안에는 update TIL/log end 로 저장한 커밋까지 올라와있다.)
* d274f1d (origin/main) update TIL (깃허브 원격저장소 origin/main 에는 그전 단계인 update TIL 로 저장한 커밋까지만 올라와있다.)
```
브랜치 병합을 하지않아서 그래프 모양은 없다.

### - reset, revert, restore : 되돌리기 3종 세트


### - git merge : 

### - git remote : 
#### - git remove :
### - git push : 

git push -u origin main : 변경사항을 원격 저장소'origin'에 올린다. ' -u '옵션은 기본push 대상을 설정하며, 로컬'main' 브랜치과 원격 'origin/main'을 연결한다. 처음 push할때 설정해두면 그이후에는 git push만으로도 알아서 origin원격저장소에 올라간다.

'git clone'으로 시작했다면 이미 원격 저장소origin과 연결되어 있기 때문에 'git push'만 사용해도된다. 

> but, 다른 원격저장소에 push하고싶다면 'git push backup main' 이런식으로 'backup'이라는 원격저장소를 직접 지정해줘야한다.

'main' 브랜치가 아닌 다른 브랜치명로 push할 경우 'git push backup feature/login'으로 브랜치명도 변경해줘야한다.

### - git pull


### - git mv : 파일명 변경 (사실 탐색기에서 변경해도된다.)
- 예시 : 파일명이 READEME.md로 오타가 나있었을때 README.md로 변경

변경 방법 : 
```bash 
 git mv READEME.md README.md
 ``` 
파일명을 변경한 후에는 스테이징 영역에 추가한다. :
```bash
git add . or git add README.md
```
현재 디렉터리의 변경된 파일들을 모두 스테이징 or README.md 파일만 스테이징 해준다. :
```bash
git commit -m "rename READEME.md to README.md"
```

