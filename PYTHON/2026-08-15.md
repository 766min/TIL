# TIL | 2026-08-15
## 공부한 내용 | python 

### | 데이터 보관함

#### - 리스트 : [ ] 대괄호를 사용

언제든 내용을 추가, 수정, 삭제할 수 있는 공간이다.
값이 바뀔 때 고유 주소id는 그대로 유지하면서 내용만 바꾼다.
- .append()는 끝에 추가하는 기능
- .extend()는 끝에 여러개 추가하는 기능(이지만 + 기호를 더 많이사용..)

리스트안의 요소가 추가, 수정, 삭제 이런 변동이 있어도 리스트 객체 자체의 고유주소id는 그대로 유지된다.

```python
fruits = ["사과","바나나","딸기"]

id(fruits)
```
```python
fruits.append("포도")
fruits[1] = '수박'

id(fruits)

print(fruits)
```
보면 fruits의 변수에 .append()메서드로 뒤에 값을 추가했지만 리스트 객체인 fruits의 고유주소는 변함이없이 동일하다. 그리고 인덱스1번값에 요소를 변경했지만 별탈없이 print문을써보면 바나나 대신 수박이 들어갔음
만약 리스트 안의 요소 주소를 보고싶다면 `id(fruits[0])`이런식으로 인덱싱을 사용한다.

#### - 튜플 : ( ) 소괄호를 사용

한 번 적으면 절대 지우거나 고칠 수 없는 읽기 전용 공간이다.
수정할 수 없는 대신 리스트보다 메모리사용량이 적고 속도가 빨라서 바뀌면안되는 중요한 설정값을 안전하게 지킬때 사용된다.

```python
fruit = ('cherry','melon')

fruit[1] = 'podo'
```
TypeError가 생긴다.왜냐면 fruit변수가 참조하는 객체가 튜플 형태인데 값을 바꿀려했기때문이다.

### | 데이터 뽑아내기

#### - 인덱싱 : 순서대로
  - 정방향 : 맨앞데이터를 0부터시작하여 번호를 매긴다.
  - 역방향(마이너스) : 맨뒤데이터부터 -1로 시작하여 번호를 매긴다.

#### - 슬라이싱 : 구간 선택하기

  - [시작:끝:증감] 시작은 포함하고 끝은 미만 포함시키지않는다. 증감값도 설정할 수 있다.

| 시쿼스 공통 연산 : 순서가 있는 자료형들이 쓸 수 있는 연산

- 더하기 : 리스트나 튜플끼리 더해서 새로운 묶음을 만들수있다.

`a = [1, 3] b = [2, 4] print(a + b)`를 하면 결과는 [1, 3, 2, 4]

- 곱하기 : 지정한 숫자만큼 데이터를 복제한다.
`a = [1, 2] print(a * 3)` a를 3번 반복해라여서 출력은 [1, 2, 1, 2, 1, 2]가 된다. 문자열도  `a = ['hi','hello']
print(a * 3)` 하면며 ['hi', 'hello', 'hi', 'hello', 'hi', 'hello']출력이된다.

- len()함수 : 데이터의 총 갯수를 세어준다.
```python
fruits = ['apple','banana','orange']

print(len(fruits))
```
결과는 3 이 나온다.왜냐 리스트안에 요소가 3개 있기때문이다.!!

만약 문자열이라면
```python
a = 'apple'

len(a)
```
결과값으로 5가 출력된다. 왜냐 문자 하나하나의 갯수를 보기때문에 공백도 문자 포함시키는거 잊지말기

- in/not in : '이 데이터가 그 안에 들어있니?'라고 물어보고 맞으면 True 틀리면 False 를 알려준다.(조건문에 적합함)

`num = [1, 2, 3, 4, 5]` 

`print(3 in num)` num안에 3 있냐? True

`print(33 in num)` num안에 33 있냐? False

`print(33 not in num)` num안에 33 없재?True

`print(2 not in num)` num안에 2 없제?False


### - 데이터를 묶고 풀기

#### - 패킹 : 여러개의 값을 컴마로 나열해서 하나의 변수에 담으면 알아서 튜플로 묶인다.

`eng = 'a', 'b', 'c'` `print(eng)`각각의 a, b, c를 하나의 튜플로 묶었다. 결과는 ->  `('a','b','c')`

#### - 언패킹 : 묶여 있는 데이터 주머니를 풀어서 여러 개의 변수에 각각 나눠 담는다.

`fruits = ('apple','banana','cherry')` `a, b, c = fruits` 를 했을때 a b c 각각 인덱싱 순서대로 값을 담는다. 그래서  a를 입력하면 apple이 출력되고 b를 입력하면 banana가 출력되고 c도 마찬가지다.

- 가변 언패킹 : 담을 변수가 부족할 때 변수 이름앞에 * 별표를 붙이면 남은 데이터들이 알아서 뭉쳐 하나의 리스트로 포장된다.
`fruits = ("apple", "banana", "mango", "orange")` `a, *b = fruits`를 하면 a에는 apple이 담기 b에는 banana, mango, orange가 다 딤기게된다.

#### - 키(Key)와 값(Value), 딕셔너리

딕셔너리란? 키 : 값을 한 쌍으로 저장하는 자료형
 ```python
student = {
    "name": "철수",
    "age": 20,
    "score": 95
}
 ```
- keys()메서드 : "name" "age" "score" : 값을 찾기 위한 이름표 

- values()메서드 : "철수" 20 95 : 실제로 저장된 값

- items()메서드 : `print(student.items())` -> ("name", "철수")("age", 20)("score", 95)` 이렇게 key와 value를 한 쌍씩 가져온다.

- get()메서드 : `print(student.get("name"))` -> 철수  이렇게  key를 이용해서 value를 가져온다. 만약 그 key값이 없으면 오류가 아닌! None을 반환한다.

#### - 집합(Set) 자료구조 : 중복을 허용하지 않는 데이터 묶음
-  set : { } 중괄호 안에 중복을 허용하지 않는 데이터 묶음

[ 중복 x ]
```python
num = {1, 2, 3, 4, 4, 3}
num
```
-> {1, 2, 3, 4} 와 같이 중복된 값을 넣어도 자동 제거하여 하나만 남는다.

[ 순서 x ]
```python
num = {1, 2, 3, 4, 4, 3}
num[1]
```
인덱싱 1을 출력할려했지만 { }안에있는 이 set구조는 순서가 없기때문에 결과적으로 오류가 난다.

[ 주요 기능 : 값을 넣고, 빼고, 있는지 확인하기]
`num = {1, 2, 3}`
`num.add(4)` {1, 2, 3, 4}
`num.add(2)` {1, 2, 3, 4}
`num.remove(2)` {1, 3, 4}
`num.discard(5)` 얘도 지우는 기능인데 지울 요소가 없더라도 에러는 발생시키지 않는다.
`print(3 in num)` True
`print(33 in num)` False

#### - 조건문
- 조건문 끝에는 콜론을 꼭 붙여야한다.
- 실행할 코드는 들여쓰기한다.
- else는 조건을 쓰지 않는다.
- elif는 여러번 사용할 수 있다.
- if문 : 조건이 맞을 때만 코드를 실행

```python
age = 23

if age >= 20:
  print("성인")
```
age라는 변수에 23을 담아놨는데 if문으로 age값이 20이상일때 다음코드로 넘어가서 성인을 출력해라. => age >= 20이니까 True => 성인이 출력된다.

- if-else 조건문 : 조건이 맞을때와 틀릴때 둘다 정해놓음

```python
age = 19

if age >= 20:
  print("성인")
else:
  print("미성년자")
```
age변수 값으로 19를 할당했고 if문으로 age값이 20이상이면 성인을 출력하고 그렇지 않으면 else문이 있기때문에 미성년자를 출력한다.

- if-elif-else 다중 조건문 : 조건이 여러개일때

```python
score = 78

if score >= 90:
  print("A")
elif score >= 75:
  print("B")  
elif score >= 65:
  print("c")
else:
  print("D")

```
score점수가 78이기때문에 결과적으로는 B를 출력한다.

- 중첩 조건문

```python
age = 21
score = 85

if age >= 20:
    if score >= 80:
        print("합격")
```
if문안에 if문이 또 들어가는 중첩 조건문

```python
age = 21
score = 85

if age >= 20 and score >= 80 :
  print("합격")
```
and연산자를 이용해 간단하게 줄일 수 있다.

[문제]

조건:
점수가 80점 이상이면
그 안에서 출석일수가 80 이상인지 확인
둘 다 만족하면 "합격"
점수는 만족하지만 출석이 부족하면 "출석 부족"
점수부터 부족하면 "점수 부족"

[내가 생각한 오답]
```python
score = 85
attendance = 90

if score >= 80 and attendance >=80:
  print("합격")
if score >= 80 or attendance >= 80:
  print("출석 부족")
else:
  print("점수 부족")
```
중첩 조건문의 구조를 잘 이해하지 못한것같다. 저렇게 코드를 입력하니 합격과 출석 부족이 동시에 출력됐다. or 조건문도 둘 중 하나만 만족해도 True를 출력하는 조건문인데 아예 잘못 사용하였다.

[정답]
```python
score = 85
attendance = 90

if score >= 80:
  if attendance >= 80:
    print("합격")
  else:
    print("출석 부족")
else:
  print("점수 부족")
```

#### - 반복문

- for문 : 같은 작업을 여러번 반복하기

`for i in [1, 2, 3]:` `print(i)`

[1, 2, 3]에서 값을 하나씩 꺼내서 i에 넣고 반복 실행하기

- range()함수 : 지정한 범위의 숫자 꺼내주기

`for i in range(5):` `print(i)`

0부터 4까지 꺼내기

`for i in range(1, 101):` 
1부터 100까지 꺼내기

- while문 : 조건이 True인 동안만 계속 반복하기

```python
num = 1

while num <= 5:
  print(num)
  num += 1
```
1
2
3
4
5
1씩더하다가 값이 6이 되면서 조건이 False가 되어 종료된다.

- 무한 루프 : 반복문이 끝나지 않고 계속 실행되기 : 특히 While문에서 조건이 계속 True이면 무한 루프가 발생한다. 위의 코드에서 `num += 1`가 없었다면 계속 num이 1이어가지고 True.. 끝나지않는 무한반복이었다.

- break와 continue : 반복문 중간 제어

  - break : 반복문 자체를 끝내기

```python
for i in range(5):
    if i == 3:
        break
    print(i)
```
0
1
2 를 출력하고
i가 3이되는 순간 break를 만나서 반복문 자체가 종료된다.

  - continue : 이번 반복만 건너뛰기
```python
for i in range(5):
    if i == 3:
        continue
    print(i)
```
0
1
2
4
i가 3일때 continue를 만나서 3을 출력하지않고 건너띄고 4까지만 출력한다. 5는 빼고.

- 이중 반복문
```python
for i in range(2, 4):
  for j in range(1, 10):
    print(f"{i} x {j} = {i*j}")
```



### | 데일리 퀘스트 풀이 | 
주말이니 넘어가유