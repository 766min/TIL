# TIL | 2026-09-01
## 공부한 내용 | 머신러닝 | SQL

## | 허깅페이스 모델 실습


#### | 허깅페이스(Hugging Face)

- AI 모델 버전의 '깃허브 + 앱스토어'

- 전 세계 연구자들이 만든 AI 모델을 올려두는 저장소이자, 그 모델을 쉽게 써먹을 수 있게 해주는 도구`transformers`라이브러리를 같이 제공한다.

| 허깅페이스가 없던 시절

- A논문 모델을 쓰려면 -> A저자가 짜놓은 방식대로 코드 설치, 가중치 파일 형식 맞추기, 전처리 방식 이해하기

- B논문 모델을 쓰려면 -> 처음부터 다시 이과정 반복..

| 허깅페이스 도입 이후

- `model = AutoModel.from_pretrained("모델이름")` 이 한 줄이면 끝~!

- 모델이 곧 '다운로드 주소+설정정보' 역할을 한다. 즉, 이름만 알면 나머지는 알아서 처리된다.

#### | 파이프라인(Piepline)

- 토큰화->인코딩->모델연산->소프트맥스 확률 변환 등 추론에 필요한 전 과정을 하나로 추상화해 제공하는 상위 API다.

- `piepline("text-generation", model=..._)`처럼 태스크 이름과 모델만 주면 나머지를 알아서 처리한다.

#### | 자동 클래스 (AutoClasses)

- `AutoTokenizer`, `AutoModel`, `AutoModelForSequenceClassification` 등

- 모델 이름 문자열만 `from_pretrained("...")`에 넘기면, 그 모델에 맞는 전용 아키텍처와 가중치를 자동으로 다운로드.설정한다. Pipeline보다 한 단계 낮은, 토큰화.추론을 직접 제어하는 수준의 API다.


| Pipeline과 AutoClasses의 추상화 정도의 차이

- 이 둘은 같은 작업을 다른 높이에서 다루는 도구

- `pipeline`은 
  
  - 재료 손질부터 조리까지 다 되어 있어서 텍스트만 넣으면 결과가 바로 나온다.

  - 빠르게 써보기 좋음

  - 다 감싼 상위 API로 빠르게 써 보기 좋다.

- `AutoTokenizer` + `AutoModel..`은 토큰화 결과.로짓을 직접 만지므로, 확률 분포를 뽑거나 커스텀 후처리를 할 때 쓴다.

- `AutoClasses`는

  - 토큰화 결과, 확률값을 직접 눈으로 보고 만질 수 있다.

  - 중간 결과를 커스터마이징하고 싶을 때 필요하다.

  -> 즉, 그냥 빨리 결과만 보고싶다면 Pipeline. 확률값을 직접 다루거나 세밀한 제어가 필요할때는 AutoClasses를 사용하면 된다.


#### | 토크나이저 (Tokenizer)

- 모델의 임베딩 레이어는 가공되지 않은 텍스트를 직접 처리하지 못하므로, 텍스트를 정해진 사전에 맞게 단위로 쪼개고 각 조각을 고유 정수 ID로 매핑하는 전처리기


- 모델마다 학습에 쓴 사전이 다르므로, 토크나이저는 반드시 그 모델과 짝이 맞아야 한다.

| 왜 토크나이저가 모델과 짝이어야 하는가

- 모델의 임베딩 레이어는 '정수ID -> 벡터'로 룩업 테이블이다.

- 이 테이블을 특정 사전(어떤 토큰이 몇 번인지)에 맞춰 학습됐다.

- 다른 토크나이저가 만든 ID를 넣으면 엉뚱한 벡터를 꺼내게 된다.

- 그래서 KoBART는 `AutoTokenizer`가 아니라 전용 `PreTrainedTokenizerFast`가 쓴다.

### | 서브워드 분리 알고리즘 3형제

- BPE : 그냥 제일 자주 붙어다니는 글자쌍부터 합침

  - '이 둘이 제일 자주 만나네? 무조건 합치자'

- WordPiece : 합쳤을 때 언어모델 성능(우도)이 얼마나 좋아지는지 계산해서 합침

  - '자주 만나긴 하는데..합치는게 실제로 도움될까?'따져보고 합침

- SentencePiece : 띄어쓰기 자체를 특수문자(_)로 취급, 언어에 상관없이 통째로 학습

  - 아예 띄어쓰기 규칙 자체를 무시하고 처음부터 다시 설계

#### | BPE (Byte Pair Encodding)

- 데이터에서 가장 빈번하게 등장하는 글자(바이트) 쌍을 반복적으로 병합해 어휘 사전을 구축하는 알고리즘

- 순수 빈도 기반이며, GPT,Llama,Gemma 등 현대 주요 LLM의 기반이 된다.

#### | WordPiece

- BPE와 비슷하게 반도 기반이지만, '이 쌍을 병합하면 전체 코퍼스의 우도(Likelihood)가 얼마나 올라가는가'를 통계적으로 계산해 병합을 결정한다.

- 단순히 자주 겹치는 쌍이 아니라 '합쳤을 때 이득이 큰 쌍'을 고른다. BERT.KoBERT에 쓰인다.

| BPE vs WordPiece

- BPE는 코퍼스에서 가장 자주 인접하는 글자 쌍을 무조건 병합한다.

- WordPiece는 한 걸음 더 나아가, 그 병합이 언어 모델의 우도를 얼마나 끌어올리는지르 계산해 '이득이 큰'쌍부터 병합한다. 결과 사전이 조금 달라진다.

#### | SentencePiece

- BPE.WordPiece와 달리 문장에 미리 띄어쓰기가 없어도 처리 가능한 독립형 전처리 라이브러리(구글)다.

- 공백 자체를 특수 기호(예: _ )로 취급하므로, 띄어쓰기 규칙이 불규칙한 한국어나 공백이 없는 중국어.일본어에 효과적이고 언어 독립적이다.

| 한국어에 강한 이유

- BPE.WordPiece는 먼저 공백으로 단어를 나눈 뒤 그 안에서 서브워드를 만든다. 

- 한국어는 띄어쓰기가 불규칙해 이 전재가 자주 깨진다.

- `SentencePiece`는 공백까지 하나의 문자(_)로 보고 통째로 학습하므로, 띄어쓰기 규칙에 의존하지 않는다.

### | 인코더/디코더/인코더.디코더 - 구조에 따른 특기

- 이 셋의 차이는 '어느 방향을 볼 수 있나'로 이해하면 쉽다.

#### | 인코더 단독 모델 (KoBERT)

- 문장 전체의 컨텍스트를 양방향으로 동시에 분석(Self-Attention)하는 구조

- 문장의 전반적 의미를 도출하는데 강해, 감성 분석.개체명 인식 같은 자연어 이해(NLU) 태스크에 적합하다.

#### | 인코더-디코더 모델 (KoBART)

- 입력을 인코더로 맥락화한 뒤, 그 맥락을 바탕으로 디코더가 새 문장을 생성하는 구조

- 기계 번역.문서 요약처럼 '입력을 다른 형태의 출력으로 바꾸는'태스크에 쓴다.

#### | 디코더 단독 모델 (Gemma)

- 이전 텍스트 토큰들을 기반으로 뒤이어 올 가장 적절한 토큰을 인과적으로(미래를 못 보고)예측해 나가는 구조

- 생성형 대화 에이전트(NLG)의 핵심

#### | 로짓 -> 소프트맥스 (분류 확률 변환)

- 분류모델 `(AutoModelForSequenceClassification)`의 출력 `model_outputs.logits`는 범위 제한 없는 원시 점수다.

- `torch.nn.functional.softmax(logits, dim=-1)`로 합이 1인 확률 분포로 바꾼 뒤 레이블에 매핑한다.

-> 분류 모델이 뱉는 원시 출력logits은 범위제한없는 숫자로 확률이라고 부를 수 없으니 softmax를 씌워서 확률값으로 바꿔준다.

  - 소프트맥스는 큰 값은 더 크게, 작은 값은 더 작게 벌려주면서 확률 분포로 정규화하는 함수다.

#### | device_map="auto" / accelerate

- `pipeline`이나 `from_pretrained`에 `device_map="auto"`를 주면, `acclerate`패키지가 실행 환경을 판별해 최적 가속 레이어(NVDIA는 CUBA, Mac은 MPS, 없으면 CPU)에 모델을 자동 배치

- `dtype=torch.bfloat16`은 메모리를 아끼는 저정밀 연산 타입










### | 데일리 퀘스트 |

Q1. 다음 중 SentencePiece 토크나이저의 특징으로 옳은 것은 ?

A. [내 오답] c.통합했을 때 코퍼스 우도(Liklihood)를 통계적으로 계산하여 분할 여부를 결정한다.

ㄴ>이거는 WordPiece의 설명 !

[ 정 답 ] - SentencePiece는 공백 자체를 특수 기호(예:_)로 처리하는 독립형 전처리 라이브러리로, 띄어쓰기 규칙이 불규칙한 한국어나 공백이 없는 중국어.일본어에 효과적이다.

---

Q2. 트랜스포머의 인코더.디코더 결합 구조가 주로 활용되는 대표적인 자연어 처리 태스크를 한 가지 이상 쓰시오.

A. [ 내 오답 ] - KoBERT

[ 정 답 ] - KoBART

-> 인코더-디코더 구조는 입력 문장을 인코더로 맥락화한 뒤 디코더가 새로운 문장을 생성하는 방식이므로, KoBART 실습처럼 문서 요약이나 기계 번역에 주로 사용된다.

---

Q3. 허깅페이스 pipeline에서 deveice_map="auto"옵션을 설정했을 때, accelerate패키지가 수행하는 역할로 가장 적절한 것은?

A. b. 운영체제 환경을 판별하여 CUBA 또는 MPS 등 최적의 가속 자원을 자동으로 매핑한다. ( 정 답 )

-> accelerate패키지는 OS환경을 판별하여 CUBA(NVDIA) 또는 MPS(Mac) 등 최적의 가속 레이어를 자동으로 매핑하는 역할을 한다.

---

Q4. 디코더 전용 모델이 문장을 생성할 때, 현재 시점보다 미래에 위치한 단어를 참조하지 못하도록 어텐션 스코어의 상삼각 영역을 차단하는 기법의 이름은 무엇인가?

A. [ 내 오답 ] - 마스킹

[ 정 답 ] - 인과적 마스킹(Casual Masking)은 어텐션 스코어 행렬의 상삼각 영역을 마스킹하여 모델이 역방향(미래 시점) 정보를 참조하지 못하게 하는 기법으로, Gemma와 같은 디코더 전용 모델의 인과적 텍스트 생성을 가능하게 한다.

---

Q5. 관계형 데이터베이스(RDBMS)가 계층형.망형 데이터베이스와 비교하여 갖는 특징으로 가장 적절한 것은 ?

A. c.데이터를 테이블(행.열)형태로 저장하여 유연성이 높고 SQL로 조회할 수 있다.( 정 답 )

-> 관계형(RDBMS)은 테이블 형태로 데이터를 저장하여 유연성이 높고 SQL로 조회 가능하다는 특징이 있다.

