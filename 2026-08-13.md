# TIL | 2026-08-13
## 공부한 내용 | python | 데일리 퀘스트

### python 가상환경 uv

#### - 파이썬 가상환경의 필요성
> 파이썬을 처음 설치할때 내 컴퓨터 전체에 무심코 설치하면 나중에 실무에서 여러 프로젝트를 진행할때 파이썬 버전의 차이를 둬야할상황이 있는데 새로 패키지를 설치하거나하면 프로그램이 도미노처럼 망가니는 버전 지옥! 환경 충돌이 생긴다. 그래서 프로젝트마다 독립된 가상환경을 격리해야한다.

## - uv

  - uv init : 프로젝트를 초기화하는 명령어

### [uv init 실행 후 자동으로 생성되는 파일 구성]

#### - 'pyproject.toml' : 프로젝트 이름, 버전, 라이브러리 의존성을 선언적으로 기록하는 모던 파이썬 표준 명세서

#### - ' .python-version' : 파이썬 버전을 명시해두는 텍스트 파일

    - 'uv init --python 3.13' 식으로 3.13버전의 파이썬이 반영되고 ' .python-version'파일에 기록된다.

    - 이미 생성된 프로젝트의 버전을 변경할때 : 'uv python pin 3.12' 처럼 명령어를 입력하면 변경 가능하다.
    

#### - 'main.py' : 파이썬의 첫번째 실행 파일

    - 파일을 열면 자동으로 파이썬 진입점 함수 구조를 갖춘 코드가 입력되어있다. 

- uv run 명령어를 실행하면 uv가 스캔하여 컴퓨터에 지정된 버전의 파이썬이 아니거나 가상환경폴더인 .venv가 존재하지 않는다면 자동으로 구축까지 해준다. ex 'uv run main.py'

  - Hello from first-project! 출력결과가 터미널에 나타난다.

  - uv add : 패키지를 설치하고 의존성 리스트에 자동 기록한다.

  - uv venv : 가상환경 폴더

    - 가상환경 진입 명령어 - Windows : ' .venv\Scripts\activate'
    mac : 'source .venv\bin\activate 실행하면 터미널 프롬프트 젤 앞에 프로젝트 폴더 이름이 괄호형태로 표시되어있다.

## - 대화형 셀 REPL 

> Read(코드입력) > Eval(파이썬이 바로 계산) > Print(바로 출력) > Loop(반복)

구동방법 | 

가상환경이 활성화된 터미널에 'python'을 입력만하면 프롬프트가 >>> 바뀐다. 바로 간단한 사칙연산을 시키거나 할 수 있다.

탈출방법 | 

'exit()' 입력하면 바로 탈출 가능!

한계 | 탈출하는순간 파이썬과의 대화 기록이 메모리에서 사라져서 영구적을 보존하거나 프로젝트를 만들땐 부적합하다.

## - 스크립트 구동 방식 .py

파이썬 인터프리터에게 "이 파일을 처음부터 끝까지 한번에 실행해줘"라고 명령하는 방식 ! 코드를 영구적으로 보존가능하다.

## - REPL + 스크립트 = 주피터 노트북 .ipynb

- 코드를 독립된 블록인 '셀'로 나누어 필요한 부분을 원하는 타이밍에 실행할 수 있는 구조이다. -> 개발 생산성을 끌어올리는 획기적인 에디터

- vscode에서 확장 설치 가능하고 
- 'ipykernel' : 주피터와 파이썬을 연결시켜주는 중간다리 역할
- 가상환경이 활성화된 터미널에 'uv add ipykernel'을 입력하면 pyproject.tom에 의존성이 자동기록된다.


### | 데일리 퀘스트 풀이 |

#### Q1. LEGB 스코프 구조에서 변수 검색 우선순위가 가장 높은 영역과 가장 낮은 영역을 올바르게 짝지은 것은 무엇인가요?

A : 지역 스코프(Local) - 내장 스코프(Built-in)

ㄴ> LEGB 스코프는 좁은 범위가 우선순위가 가장 높다

지역변수(Local) > 외부 함수 변수(Enclossing) > 전역 변수(Global) > 내장 변수(Builtin)

---

#### Q2. 다음 코드가 실행될 때 최종 print(counter)의 출력 결과는 무엇인가요?
```python
counter = 0

def increase():
    global counter
    counter += 1

    def local_increase():
      counter = 100

increase()
increase()
local_increase()

print(counter)
```
A : 2

ㄴ> 코드풀이 : 전역변수인 counter 에는 0이 담겨있다 그리고 increase함수를 만들고 일단 매개변수는 넣지않았다. 다음으로 global counter은 전역변수인 counter값을 사용해라 그래서 counter += 1은 0+1이된다. 그리고 안에서 local_increase함수를 만들었고 그안의counter 변수에는 100을 넣는다. 그리고 함수호출에서는 increase함수를 두번호출했기때문에 counter에는 2가 담겨있다. local_increase함수에서 counter변수에 100이 담겼지만(실행된것) print문에서 사용되고있는 counter는 함수 밖의 전역변수이기때문이다. 그리고 100을 출력원했다면 내부함수안에서 print(counter)를 했어야했다.

---

Q3. def 키워드 없이 lambda 매개변수: 표현식 형태로 작성하며, 이름이 없는 한 줄짜리 함수를 무엇이라고 부르나요?

A : lambda 함수

ㄴ> lambda는 이름이 없는 익명 함수를 만들수있다.

```python
add = lambda x, y: x + y
```
와
```python
def add(x, y):
  return x + y
```
비슷하다 
그렇다면 굳이? 왜냐면 함수를 딱 한번 간단하게 사용할 때 이름까지 만들어주는게 번거롭기 때문이다.

---

Q4. numbers = [1, 2, 3, 4, 5]에 대해 다음 코드를 실행했을 때 result의 값은 무엇인가요? 

```python
numbers = [1, 2, 3, 4, 5]

result = list(filter(lambda x: x % 2 == 0, map(lambda x: x + 1, numbers)))
```

A : [2, 4, 6]

ㄴ> map() -> filter() 순서로 보면 쉽다.(안쪽에서부터보기)
코드 풀이 : map은 '리스트의 각각의 값에 어떤 작업을 해라' 그래서 리스트인 numbers의 각각의 값을 하나씩 map이 꺼낸다.
lambda x: x + 1 는 들어온 숫자 x에 1을 더해라 그래서 첫번째 숫자 1이 들어와서 2, 그다음 2가 들어오고 3이되고, 쭉쭉 5까지 들어와서 결국엔 [2, 3, 4, 5, 6]이 된다. 그다음 filter는 '내가 정한 조건에 맞는 숫자만 남겨줘' 해서 조건은 lambda x: x % 2 == 0 이다. 즉, x를 2로 나눈 나머지가 0 이면 그니까 짝수면 남겨라.는거다. 그래서 map의 결과인 [2, 3, 4, 5, 6]에서 하나씩 검사를 하는것임. 2 % 2 = 0이니 통과. 다음 3 % 2 = 1이니깐 3은 버려버림 해서 6까지 검사를 해서 [2, 4, 6]만 남는다. 마지막으로 지금 list()는 안의 조건 결과를 리스트로 만들어주고 그값을 result에 넣는다. 그래서 출력은 [2, 4, 6]이 된다.

---

Q5. 클로저(Closure)의 동작 방식에 대한 설명으로 가장 적절한 것은 무엇인가요?

A : 외부 함수의 실행이 완전히 종료된 후에도 내부 함수가 외부 함수의 변수 값을 기억하고 유지하는 구조이다.

[강의 예제]
```python
def outer(num1):
  def inner(num2):
    return num1 + num2
  return inner
```
```python
a = outer(10)
a(30)
```
의 결과값은 40이 출력된다.

ㄴ> 코드 풀이 : outer 이름으로 함수를 만들었고 num1을 매개변수로 넣어놓았다. 그리고 아직 실행하지않고 안에 inner이름으로 함수를 만들고 여기에는 num2를 매개변수로 넣어놓는다. 그리고 return문을 만나서 num1과 num2의 값을 더해서 반환해라 그리고 외부함수 outer의 return문은 inner함수를 실행하는것이 아니라 inner함수 전체를 반환한다. 다음 코드에서 a라는 변수에 outer(10) outer함수값을 a변수에 저장하는것이다. num1매개변수에는 10인자가 전달됐고, outer()가 inner함수를 반환하기때문에 'inner함수 자체를 가져가' a에는 반환된 inner함수가 저장된다. 다음 a(30)이 사실상 inner(30)을 실행하는거다. inner함수의 num2에 들어가서 10 + 30 이된다. 그리고 num1에는 계속 10이 저장되있는것이다. 따라서 결과는 40이 출력되는것이다. 

### | 회고
// 해야하는일
개념보다는 언제/ 어떻게 왜 사용하는지를 중심으로 작성해라

틀리거나 헷갈린이유

틀리거나 헷갈린 문제에서 무러본₩ 개념에 대한 정리

모든 문제에 대해 하나하나 왜?를 더 붙여보자. //

#### 사실 : 어떤 일이 있었나요 
TIL에는 어제 github파트를 다 정리하고 파이썬 가상환경 내용을 복습하면서 정리했지만 벌써 강의는 파이썬함수파트를 하고있다. 빨리 내 TIL에 기록하고싶은데 아직 순서대로 정리할게 너무많아서 막막하다. 그리고 함수를 열심히 따로 복습하고있지만 그코드는 겨우 이해했지만 응용문제거나 내가 다시 코드를 짤때 막힘을 느꼈다. 또 문길래 멘토님께서 색다른 방향으로 연사를해주셧다.
#### 발견 : 그 속에서 무엇을 알게되었나요 
연변디지털대학교?에서 개발기술을 배우는분들의 일과영상을 보여주셨는데 새벽6시쯤일어나서 운동을 다같이하고 하루종일 공부를하다가 밤늦게 자는 그런 일과였다.
#### 배운 점 : 뭘 배웠나요
나도 더 분발해야겠다. 잠을 일부러 늦게 잘려하지않고있었는데 지금 잠잘시간도 부족한것같다.
#### 선언 : 배우고 알게된것들로 앞으로 무엇을 하고싶나요
일댠 이번주말에 함수파트까지 다 완벽정리 해놓고싶다. 그리고 연휴 월요일에는 코드짜는걸 중점으로많이 해봐야겠다.