# TIL | 2026-08-19
## 공부한 내용 | python 

#### | 이터레이터와 제너레이터

- 이터러블 : 반복 가능한 객체
  - \_\_iter\_\_만 정의되어 있으면 반복이 가능하다.
- \_\_next\_\_ : 인덱스를 한칸씩 이동하면서 꺼내올수있다.
  - `nums = [10, 20, 30]` `nums.__iter__`이렇게 nums 의 내부에 (온점찍고) \_\_iter\_\_가 정의되어있으면 반복이 가능하다.

- 이터레이터 : 그 반복을 해주는 놈
`fruits = ['Apple', 'Orange', 'Melon', 'Banana']` `it = iter(fruits)`여기서 이제 `next(it)`를 해주면 'Apple'부터 하나씩 순서대로 출력되고 마지막 리스트인 'Banana'까지 출력이되면 끝이다 그래서 그이후에는 에러가 뜬다. fruits리스트 자체가 이터러블이 되는거고 it 변수가 이터레이터가 되는것이다.

  - 여기서 iter()와 \_\_iter\_\_는 다른 역할!!! : iter()는 파이썬이 제공하는 일반 함수이고 iter(fruits)를 실행하면 결국엔 `fruits.__iter__()`를 호출하는 방식으로 동작된다.
  - 던더 메서드 : \_\_iter\_\_ 이런 형태를 던더 메서드라고 한다.
    - 저런 던더 메서드를 사용할때는 `fruits.__iter__()`라고 쓰는게 아닌 iter(fruits)라고 입력하면 자동으로 파이썬이 iter()를 찾아 호출해준다.
    - \_\_next\_\_ 도 next(it)라고 입력한 이유는 결국엔 `it.__next__()`를 호출하는모양이기때문!!

- 제너레이터 : 데이터를 미리 전부 만들어두는게 아닌 필요할때마다 하나씩 즉석에서 만드는 함수이다.
```python
def generator():
  yield "대박"
  yield "사건"
  yield "성공"
```
이렇게 함수를 만들고 출력을할때
```python
gen = generator()

next(gen)
next(gen)
next(gen)
```
이렇게 next(gen)을 세번해주면 순서대로 출력된다. 여기서 몰랐던점 ! 이터러블 처럼 next를 넣어주면 자동으로 하나씩 넘어갈줄알았는데 이건 제너레이터함수이므로 그런 반복을 하는 이터러블과 다르다!! 제너레이터는 필요할때마다 하나씩 즉석에서 만드는함수임을 기억하기.

- 제너레이터 표현식 (리스트 컴프리 헨션과 비교)

`gen = (i for i in range(1, 101) if i % 2 == 0)`

range(1, 101) 은 1부터 100까지 숫자를 반복하고 그거를 하나씩 꺼내서 i 에 넣겠다. 그러면 i = 1 그다음은 i = 2 그다음은 i = 3 이렇게 쭉쭉쭉 100까지 담기는데 if 문은 i에 담긴 숫자를 짝수인지 확인하는 조건이다. 그리고 젤 앞의 i는 이 조건을 통과한 i를 하나씩 내보내라 즉, 처음부터 2, 3, 6, 8, ... 짝수가 만들어져서 저장하는게 아니라 필요할때마다 하나씩 만들어서 내보내는것 그래서 print(next(gen))을 하게되면 처음에는 2만 출력 다음엔 4만 출력 다음엔 6, 다음엔 8 이렇게 100까지 하나씩만 출력된다.

### | 클래스

- 클래스 : 객체를 만드는 설계도

예. 강아지를 만들겠다고 했을때 
```python
class Dog:
  pass
```
여기서 Dog은 강아지를 만들기 위한 설계도 
```python
dog1 = Dog()
dog2 = Dog()
```
이렇게 객체를 만든것이다. 실제 객체가 두개 생겼고 이 dog1, dog2가 객체(인스턴스)인 것이다. 이 객체는 자기만의 데이터를 가질 수 있다.
```python
class Dog:
  def __init__(self, name):
    self.name = name


dog1 = Dog("초코")
dog2 = Dog("콩이")
```
이렇게 아까 pass대신 함수값을 입력했고 .. 이 내용은 Dog라는 같은 이름으로 클래스가 만들어졌지만 각 객체 dog1, dog2는 자기 데이터를 따로 가지고 있다. 또 여기서 기능을 추가해보겠다.
```python
class Dog:
  def __init__(self, name):
    self.name = name

  def bark(self):
    print(self.name, "멍멍")



dog1 = Dog("초코")
dog2 = Dog("콩이")

dog1.bark()
dog2.bark()
```
[ 총 풀이 ]

Dog라는 클래스를 만들었고 def로 함수를 만드는데 클래스안에서 만들어지는 함수이므로 이를 메서드라고 부른다. \_\_init\_\_는 파이썬이 특별하게 사용하는 메서드로 객체를 만들 때 자동으로 실행이된다. `(self, name)` 여기서 self는 지금 만들어지고 있는 객체 자기 자신이다. 밑에서 `dog = Dog("초코")`를 하면 self가 dog1이 되는것.. dog2도 마찬가지! 또 괄호안의 name은 매개변수이고 Dog("초코")를 호출했으니 name에는 초코가 들어오게된다. 그다음 `self.name = name` 등호를 기준으로 오른쪽의 name이 방금받은 매개변수 즉 초코가 되는거고 왼쪽의 self.name은 객체가 가지고 있는 name이라는 속성.. 결국 dog1 = Dog("초코")를 실행했을대 self가 dog1이되고 name은 초코-> `dog1.name = "초코"` 가 된다.~!

이렇게 \_\_init\_\_에는 객체가 처음 만들어질때 필요한 정보를 넣어주는 역할이다. 이 객체가 처음 만들어질대 어떤 정보를 가지고있게할지를 정하는 공간이다. 그래서 이코드에서는 이름정보 name을 넣어준것!

그다음으로 bark라는 (함수)메서드를 만들고 여기는 객체가 만들어진다음에 사용할 수 있는 기능을 넣는 공간이다.

그렇다면 왜 이름을넣고 기능을넣는거를 클래스밖에서 하는건가 그것은 클래스 안에서는 설계를 하는것뿐이고 실제값은 클래스 밖에서 만드는것이다.








### | Python 과제 | 8장 1강 : 클래스

문제 1. 도서 정보 출력 프로그램

[ 조건 ]
1. Book이라는 이름의 클래스를 정의하세요.
2. 클래스 내부에 인스턴스가 생성될 때 자동으로 호출되는 __init__ 메서드를 정의하세요.
3. 초기화 함수(__init__)는 첫 번째 매개변수로 self를 지정하고, 추가로 title(제목)과 author(저자)를 매개변수로 받아 객체의 속성으로 선언하세요.
4. 클래스 내부에 info라는 이름의 인스턴스 메서드를 정의하세요.
5. info 메서드는 self.title과 self.author를 활용하여 "책 제목: [제목], 저자: [저자]" 형식의 문장을 출력하도록 구현하세요.
6. 클래스 외부에서 서로 다른 제목과 저자 값을 가지는 2개의 도서 객체(인스턴스)를 각각 생성하세요.
7. 생성된 각각의 도서 객체에서 info 메서드를 호출하여 데이터가 서로 섞이지 않고 개별적으로 관리되는지 확인하세요.





문제 2. 스마트 조명 제어 클래스 설계

[ 조건 ]
1. SmartLamp라는 이름의 클래스를 정의하세요.
2. 초기화 함수 __init__ 메서드를 정의하고, 첫 번째 매개변수로 self를 지정하세요.
3. 초기화 함수는 model(모델명)을 매개변수로 전달받아 속성으로 설정하고, 전원 상태를 나타내는 is_on 속성은 초기 기본값으로 False를 할당하세요.
4. 클래스 내부에 조명을 켜는 기능을 수행하는 turn_on 인스턴스 메서드를 정의하세요.
5. turn_on 메서드는 실행 시 현재 객체의 is_on 속성을 True로 변경하고, "[모델명] 조명이 켜졌습니다."라는 문장을 콘솔에 출력하도록 구현하세요.
6. 클래스 내부에 조명을 끄는 기능을 수행하는 turn_off 인스턴스 메서드를 정의하세요.
7. turn_off 메서드는 실행 시 현재 객체의 is_on 속성을 False로 변경하고, "[모델명] 조명이 꺼졌습니다."라는 문장을 콘솔에 출력하도록 구현하세요.
8. 클래스 외부에서 스마트 조명 인스턴스를 생성하고 turn_on 메서드와 turn_off 메서드를 순서대로 실행하여 상태 변화를 제어하세요.





문제 3. 게임 캐릭터 전투 및 체력 관리 시스템 설계

[ 조건 ]
1. Hero라는 이름의 클래스를 정의하세요.
2. 초기화 함수 __init__ 메서드를 정의하여 첫 번째 매개변수로 self를 명시하고, name(이름)을 매개변수로 받아 객체의 속성으로 연결하세요.
3. 캐릭터가 생성될 때 가질 초기 체력 속성인 hp를 정의하고, 기본값으로 100을 할당하세요.
4. 클래스 내부에 외부 공격을 처리하는 take_damage 인스턴스 메서드를 정의하세요.
5. take_damage 메서드는 self 외에 추가로 damage(피해량)를 매개변수로 전달받도록 설계하세요.
6. 메서드 내부에서 현재 객체의 hp 속성을 전달받은 damage 값만큼 차감한 후, 남은 체력을 출력하세요.
7. 만약 차감된 hp 속성의 최종 값이 0 이하가 된다면 "[이름] 캐릭터가 쓰러졌습니다."라는 안내 문장을 추가로 출력하도록 조건문을 구현하세요.
8. 클래스 외부에서 캐릭터 인스턴스를 생성하고 take_damage 메서드를 호출하여 체력이 누적 차감되다가, 0 이하 조건에 도달했을 때 캐릭터가 쓰러지는 동작이 올바르게 수행되는지 확인하세요.

















### | 데일리 퀘스트 풀이 | 

Q1. 다음 중 실행 시 TypeError가 발생하는 코드로 가장 올바른 것은 무엇인가요?

[ 보기 ]

a. result = 10 / 0
b. number = int("스파르타")
c. result = 10 + "20"
d. print(numbers[5]) #numbers = [10, 20, 30]

A. result = 10 + "20" (정답맞춰써욤)
-> TypeError 는 서로 다른 데이터타입끼리 산술 연산을 시도하면 발생함.

a는 ZeroDivisionError(숫자를 0으로 나누려고 할때 발생하는 에러), b는 ValueError(타입종류는 맞는데 그안의 값이 처리할수없는형태), c는 IndexError(리스트나 튜플 등에서 존재하지 않는 인덱스위치에 접근하려고할때)가 발생함

---

Q2. try-except-else-finally 구조에서 예외 발생 여부나 성공.실패와 관계없이 무조건 마지막에 실행이 보장되는 블록의 이름은 무엇인가요?

A. finally (정답!)
-> 성공.실패 여부와 무관하게 반드시 실행되는 최종 공간이다.
try : 에러가 날 가능성이 있는 불안한 코드를 넣어두는 공간
except : try 안에서 에러가 났을때 처리하는 공간
else : try에서 에러가 나지 않고. 성공했을대 실행되는공간

---

Q3. 다음 코드를 실행했을 때 콘솔에 출력되는 내용으로 가장 올바른 것은 무엇인가요?
```python
def set_user_age(age):
  if age < 0:
    raise ValueError("나이는 음수가 될 수 없습니다.")
  print(f"사용자의 나이가 {age}세로 설정되었습니다.")
try:
  input_age = -5
  set_user_age(input_age)
except ValueError as e:
  print(f"[시스템 경고] {e}")
```
a. 사용자의 나이가 -5세로 설정되었습니다.
b. [시스템 경고] 나이는 음수가 될 수 없습니다.
c. ValueError : 나이는 음수가 될 수 없습니다.(프로그램 강제 종료)
d. 아무 것도 출력되지 않는다.

A. [시스템 경고]나이는 음수가 될 수 없습니다. (정답!!)

-> age가 음수이므로 함수 내부에서 raise ValueError가 실행되어 함수 실행이 중단되고, 예외 객체가 상위 try 블록으로 전달된다. 바깥의 except ValueError as e 구문이 이를 잡아 메시지를 출력하므로 print 구문은 실행되지 않고 경고 메시지만 출력이된다.


---

Q4. 파이썬에서 변수 이름 앞에 언더바 두개(__)를 붙였을 때, 외부에서 원래 이름으로 직접 접근하지 못하도록 이름이 자동으로 바뀌는 규칙을 무엇이라고 하나요?

A. 

[내 오답]

프라이빗함수라고 답했다..(오답처리.!)

[ 정답 ]

이름 변형 규칙이 적용되며, 원래 이름으로는 외부에서 직접 접근할 수 없게 된다.

---

Q5. 빈 리스트([])를 all()과 any()에 각각 인자로 전달했을 때의 반환 결과로 올바른 것은 무엇인가요?

[ 보기 ]

a. all([])는 False, any([])는 True
b. all([])는 True, any([])는 False
c. all([])와 any([]) 모두 True
d. all([])와 any([]) 모두 False

A. b.all([])는 True, any([])는 False (정 답!!)

-> all([])은 공백도 True이고 any([])는 공백을 False로 처리한다.