# TIL | 2026-08-16
## 공부한 내용 | python 


### 코드 직접 연습하기

Q1. 1부터 10까지의 합 구하기

조건: for문과 range()를 사용하여 1부터 10까지(10 포함)의 숫자를 차례대로 더한 총합을 출력하세요

```python
total = 0

for i in range(1, 101):
  total += i
print(f"합계 : ", total)
```
total 값에 0을 부여해야 차례대로 더할때 깔끔하게 출력된다.
처음에는 total 이라는 다른 변수를 만들어야되는걸 모르고 i += 1이라고 생각했다.

---

Q2. 구구단 특정 단 출력하기

조건: for문과 range()를 사용하여 구구단 3단을 다음과 같은 형식으로 출력하세요. (3 * 1 = 3부터 3 * 9 = 27까지)
```python
for i in range(3, 4):
  for j in range(1, 10):
    print(f"{i} * {j} = {i*j}")
```
구구단은 잘 알것같다. 젤처음배울때는 어려운 문제라고생각했는데 보면 단수와 곱해야 할 수를 따로 생각하면되는문제여서 쉽게 풀린다.

---

Q3. 리스트에서 짝수만 골라 출력하기

조건: 주어진 리스트 numbers = [12, 33, 44, 55, 68, 77, 90]에서 for문과 if문을 함께 사용하여 짝수인 숫자들만 골라서 출력하세요.

[ 내 오답 ]

```python
numbers = [12, 33, 44, 55, 68, 77, 90]

for i in range[numbers]:
  if numbers % 2 == 0:
    print(numbers)
```
리스트에서 값을 뻬내야 하는걸 잘모르겠다.

[ 정답 ]

```python
numbers = [12, 33, 44, 55, 68, 77, 90]

for i in numbers:
  num = i
  if num % 2 == 0:
    print(num)
```    

---

Q4. 문자열 속 특정 문자 개수 세기

조건: 주어진 문자열 text = "python programming is fun"에서 알파벳 'n'이 총 몇 번 나오는지 for문을 사용해서 세고, 그 개수를 출력하세요.

[ 내 오답 ]
```python
text = "Python programming is fun"

n = len(text)

for i in range(n):
```
이런식으로 접근했다..

[ 정답 ]
```python
text = "Python programming is fun"
count = 0

for i in text:
  if i == 'n':
    count += 1
print(f"n의 갯수 : ", count)
```
count = 0의 필요성을 몰랐다. 앞의 문제에서도 변수에 왜 0부터 시작한다고 알려줘야하는 과정의 필요성을 몰랐는데 컴퓨터는 일회성 기억력이기때문에 저 문장을 하나씩 보면서 'n'을 찾아기억할 공간이 필요하다.. 또 i += 1이 아닌 count += 1을 해야하는건지 헷갈렸는데 지금 저 count의 역할은 숫자를 세기 위한 역할이기떄이고 i의 역할은 글자를 담는 변수의 역할이다. 또 print문의 들여쓰기위치도 count밑에 넣어놨었는데 그렇게하면 n을 찾을때마다 출력을하라는 for문안에서의 역할이되고 문제가 원하는것은 아니었기에 다 찾고 출력하자이니 for문이 완전 끝난뒤 한번에 출력해야해서 바깥에 둬야한다는것도 알게되었다.

---

Q5. 별 피라미드 만들기 (단순 반복)

조건: for문과 range()를 사용하여 아래와 같이 총 5줄의 별(*)을 출력하세요.
[ 정답 ]
```python
for i in range(1, 6):
  for j in range(i):
    print("*", end = "")
  print()
```
[ 풀이 ]

end = "" 는 이 출력의 끝을 무엇으로 끝낼까의 옵션이므로 여기서는 따옴표사이에 아무것도 넣지 않았으므로 출력이 끝나도 엔터를 치지 말고, 그냥 그 자리에 가만히있으라는 뜻이다.

---

Q6. 배수의 합 구하기

조건: for문과 range()를 사용하여 1부터 50까지의 숫자 중 3의 배수들만 모두 더한 총합을 구하고 출력하세요.
```python
total = 0

for i in range(1, 51):
  if i % 3 == 0:
    total += i
print(total)
```


Q7. 역순으로 구구단 출력하기

조건: for문과 range()를 사용하여 구구단 2단을 거꾸로 출력하세요. (2 * 9 = 18부터 2 * 1 = 2까지 내려가기)
```python
for i in range(2, 3, -1):
  for j in range(1, 10, -1):
    print(f"{i} * {j} = {i*j}")
```
출력해야 하는건 2단 하나이기때문에 
```python
for j in range(9, 0, -1):
  print(f"2 * {j} = {2 * j}")
```
이렇게 입력해도된다. 그리고 range()메서드는 숫자를 순서대로 안해도됨.

---

Q8. 리스트 최댓값 직접 찾기

조건: 주어진 리스트 scores = [45, 89, 72, 91, 60, 84]에서 for문과 조건문(if)을 사용하여 가장 큰 숫자(최댓값)를 찾아 출력하세요. (파이썬 내장 함수 max()를 쓰지 말고 for문으로 직접 비교해 보세요!)

[ 내 오답 ]
```python
scores = [45, 89, 72, 91, 60, 84]

for i in scores:
  num = i
  if num >= 90:
    print(num)
```
이런식으로 일단 접근을 해봤지만 당연히 오답이다. 가장큰최대값을 내가 알려준셈이기때문에..

[ 정답 ]
```python
scores = [45, 89, 72, 91, 60, 84]

max_score = scores[0]

for i in scores:
  if max_score < i:
    max_score = i
print("최대값 :", max_score)
```
max_score에 리스트의 첫번째 값인 45가 들어간다. 그리고 for문에서 score에 잇는 숫자를 하나씩 꺼내서 i에게 넣는다. 그리고 if문을 통해 max_score인 45보다 i에 들어온수가 크냐?하면 True이면 다음 max_score = i 이므로 max_score에 넣어지고 False이면 다음 재검사해서 제일 큰수인 91이 담기게된다.!!

---

Q9. 특정 단어만 골라서 출력하기

조건: 주어진 리스트 words = ["apple", "banana", "kiwi", "blueberry", "grape"]에서 for문과 if문을 사용하여 글자 수가 5글자 이상인 단어들만 골라서 출력하세요. (팁: 문자열 길이는 len()을 사용합니다) 

[ 정답 ]
```python
words = ['apple', 'banana', 'kiwi', 'blueberry', 'grape']

for i in words:
  if len(i) >= 5:
    print("5글자 이상 :", i)
```


Q10. 역삼각형 별 피라미드 만들기

조건: 앞서 풀었던 별 피라미드의 반대 모양으로, for문과 range()를 사용하여 아래와 같이 총 5줄의 별을 출력하세요

[ 정답 ]
```python
for i in range(5, 0, -1):
  for j in range(i):
    print("*", end = "")
  print()
  
```

---

Q11. 짝수만 골라서 더하기
조건: numbers = [12, 7, 34, 21, 56, 9, 40] 리스트가 있습니다. for문과 if문을 사용하여 이 리스트 안의 숫자 중 짝수만 골라서 모두 더한 최종 합계를 구하고 출력하세요.

```python
numbers = [12, 7, 34, 21, 56, 9, 40]

num = 0

for i in numbers:
  if i % 2 == 0:
    num += i

print(num)
```
---

Q12. 3의 배수 찾기 게임
조건: 1부터 30까지의 숫자 중에서 3의 배수인 숫자만 골라서 한 줄로 나열해 출력하세요. (힌트: range()와 %(나머지 연산자)를 활용해 보세요!)

```python
for i in range(1, 31):
  if i % 3 == 0:
    print(i, end = " ")
```
---

Q13. 특정 글자(알파벳) 개수 세기
조건: 문자열 word = "programming"이 있습니다. for문과 if문을 사용하여 이 단어 안에 알파벳 'g'가 총 몇 개 들어있는지 세어서 출력하세요.

```python
word = "programming"
count = 0

for i in word:
  if i == 'g':
    count += 1

print(count)
```

---

Q14. 합격자 명단 뽑기
조건: 학생들의 점수가 담긴 점수 리스트 scores = [55, 78, 92, 40, 88, 65]가 있습니다. 60점 이상인 점수만 골라서 "합격 : XX점" 형태로 각각 출력하세요.

```python
scores = [55, 78, 92, 40, 88, 65]

for i in scores:
  if i >= 60:
    print(f"합격 : {i}점")
```

---

Q15. 우하향(오른쪽 정렬된) 별 역삼각형 만들기
조건: 아까 풀었던 별 피라미드의 응용 버전입니다! for문과 range()를 활용하여 아래 모양처럼 오른쪽으로 정렬된 역삼각형 별을 출력해 보세요.

```python
for i in range(5, 0, -1):
  
  print(" " * (5 - i) + "*" * i)
```
변수 i가 5부터 시작해서 1씩 감소하며 1까지 반복한다. (5, 4, 3, 2, 1 순서로 총 다섯번 반복) print문에서 " " 문자열 공백을 만들어줬고 공백이있어야 오른쪽으로 정렬을 만들어줄수있다. 공백 최고크기자리가 별 다섯개  이므로 공백자리는 5칸이 최대이면 된다 그래서 하나씩 i를 빼주면 공백이 처음엔 0칸 그다음은 1칸 그다음 2칸 그렇게 별하나출력할때는 공백이 4칸이되게끔하는것이다. i의 갯수만큼 별 문자를 곱해서 출력하는것이다. ~ 