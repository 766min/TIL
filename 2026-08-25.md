# TIL | 2026-08-25
## 공부한 내용 | python 



### | 내장 함수

#### - isinstance() : 자료형 검증
  - 지정한 변수가 내가 원하는 데이터 타입이 맞는지 판별하여 True or False를 반환해준다 
  - `age = 20` `print(isinstance(age, int))` 이렇게 하면 age변수에 있는값이 정수가 맞으니 True를 출력한다 !

#### - all() , any() : 다중 조거너 판단
  - all() : 시퀀스 안의 모든데이터가 True일때만 최종 True를 반환한다.
    - ** 빈 데이터일시 ** : 파이썬 구조상 비어있는 리스트에는 False가 단 하나도 없으므로 참이라고 판단하여 비어있지만 True를 반환한다.
    - ** 실무 대처 ** : 사용자가 아무 입력도 안했는데 약관 동의가 통과되는 참사를 막기위해 반드시`if user_inputs and all(user_inputs):`이렇게 데이터가 존재하고 모두 참인지를 2중으로 검사한다.

  - any() : 시퀀스 안에 데이터중 하나라도 True가있으면 최종적으로 True를 반환한다.
    - ** 빈 데이터일시 ** : True가 단 하나도 없으므로 거짓이라고 판단하여 False를 반환한다.


#### - len(), sum(), round() : 수치 계산과 통계 함수

- len() : 리스트나 문자열 안에 들어가는 요소의 총개수를 세어준다 
  - 예. `len([85, 90, 95])` -> 3 이 출력된다.

- sum() : 숫자가 담긴 리스트의 모든 값을 누적하여 더한 총합을 구해준다.

- round() : 실수를 지정한 소수점 아래 자리까지 반올림하여 정돈해준다. 
  - 예. `round(87.6666, 2)` -> 87.67 이렇게 소수점 둘째자리에서 반올림해서 출력함

#### - mean(), median(), mode() : 평균과 중앙값, 최빈값

[파이썬 기본 탑재 모듈인 `statistics`를 불러와서 사용한다.]

[ `import statistics` ]

- mean() : 모든 값을 더해서 나눈 일반적인 평균값
  - 이상치가 섞이면 평균이 엉망으로 왜곡될수도 있다.

- median() : 데이터를 크기 순으로 일렬로 쫙 나열했을 때 정중앙에 위치하는 값
  - 이상치가 섞여있어도 중앙값은 딱히 타격받지 않는다.

- mode() : 

#### - map(), filter() : 데이터 가공 및 필터링 함수

- map() : 모두 다 바꿔버려
  - 반복 가능한 객체의 모든 요소에 동일한 계산을 한번에 적용한다.
```python
original_prices = [1000, 2000, 3000]

discounted = list(map(lambda x: x * 0.9, original.prices))
print(discount)
```
모든 가격에 0.9씩 곱해서 리스트 모양으로 변환한것 


- filter() : 내기준에 맞춘것만 고르기
  - 데이터 목록에서 조건문이 True인 요소들만 뽑아간다.
```python
scores = [45, 80, 55, 90, 30]
passing_scores = list(filter(lambda x: x >= 60, scores))
print(passing_scores)
```
60점 이상인 점수만 걸러내서 리스트 형식으로 출력한다.

#### - enumerate() : 인덱스 자동 부여
```python
fruits = ["사과", "바나나", "체리"]

for index, fruit in enumerate(fruits, start=1):
  print(f"{index}등 : {fruit}")
```
-> 1등 : 사과
  2등 : 바나나
  3등 : 체리 이렇게 출력된다 start옵션으로 시작할 인덱스값을 지정해준거임

#### - zip() : 서로 다른 리스트를 순서대로 짝지어주기 (튜플 형태로)
```python
names = ['철수', '영희', '민수']
scores = [85, 92, 78]

for name, score in zip(names, scores):
  print(f"{name}님의 점수는 {score}점입니다.")
```
-> 철수님의 점수는 85점입니다.
  영희님의 점수는 92점입니다.
  민수님의 점수는 78점입니다. 이렇게 짝을 지어서 출력이된다.

#### - sorted() : 데이터를 오름차순 또는 내림차순으로 정렬
```python
numbers = [40, 10, 50, 30, 20]

# 오른차순 정렬 (작은거 -> 큰거)
sorted_asc = sorted(numbers)
print(sorted_asc)

# 내림차순 정렬 (큰거 -> 작은거)
sorted_desc = sorted(numbers, reverse=True)
print(sorted_desc)
```

#### - min(), max() 

- min() : 데이터 목록에서 가장 최저값

- max() : 데이터 목록에서 가장 최고값

### | 데일리 퀘스트 | 

Q1. 전통적 프로그래밍과 머신러닝의 차이를 설명한 것으로 가장 적절한 것은 ?

[ 보기 ]

a. 전통적 프로그래밍은 데이터의 정답을 넣으면 규칙을 만들어내고, 머신러닝은 규칙과 데이터를 넣으면 정답을 만들어낸다.

b. 전통적 프로그래밍은 규칙과 데이터를 넣으면 해답을 만들고, 머신러닝은 데이터와 해답을 넣으면 규칙(모델)을 만들어낸다.

c. 머신러닝은 항상 사람이 100% 직접 규칙을 짜야 하는 방식이다.

d. 전통적 프로그래밍은 상황이 바뀌어도 새로운 데이터로 쉽게 적응한다.

A. [내 오답] a.

[ 정답 ] b.
-> 전통적 프로그래밍은 사람이 짠 규칙과 데이터를 넣어 해답들 얻는 방식이고, 머신러닝은 반대로 데이터와 정답을 주면 컴퓨터가 스스로 규칙(모델)을 찾아낸다. 

---

Q2. 정답(라벨)없이 데이터만 주고 비슷한 특성을 가진 데이터끼리 그룹으로 묶어내는 머신러닝 학습 방법의 이름은 무엇인가요?

A. 비지도 학습

-> 정답 없이 데이터의 특성(거리 등)만으로 비슷한 것끼리 묶는 방식을 비지도 학습이라고 하며, 대표적인 기법이 군집화이다. 지도 학습은 라벨(정답)이 필요하고, 강화 학습은 보상 기반 상호작용이 필요하다는 점에서 구분된다.

---
 
Q3. 다음 중 '지도 학습(Supervised Learning)'중에서도 '회귀(Regression)'문제에 해당하는 사례는?

[ 보기 ]

a. 고객의 구매 패턴만으로 비슷한 고객끼리 그룹으로 묶기

b. 광고비와 계절 데이터를 바탕으로 다음 분기 매출액(숫자)을 예측하기

c. 지난 구매 기록으로 고객이 다음 달에 이탈할지(이탈/유지) 예측하기

d. 입찰가를 바꿔가며 광고 클릭 보상을 최대화하기

A. b.
-> 매출액처럼 연속적인 숫자를 맞추는 문제는 지도 학습 중에 회귀에 해당한다. a는 정답이 없는 비지도학습(군집화)이고, c는 이탈/유지 라는 범주를 맞추는 분류, d는 보상 기반의 강화 학습 사례이므로 회귀와 구분된다.

---

Q4. 타이타닉 데이터에서 '나이(age)'처럼 숫자형 데이터의 결측치를 채울 때 일반적으로 사용하는 대표값은 무엇인가요?

A. 숫자형 데이터는 산술 계산이 가능하므로 전체 데이터의 평균으로 결측치를 자연스럽게 채울 수 있다. 반면 항구 이름같은 문자/범주형 데이터는 평균을 낼 수 없으니 최빈값을 사용한다.

---

Q5. Pandas의 Copy-on-Write(CoW) 환경에서 데이터를 안전하게 수정하기 위한 방법으로 가장 적절한 것은?

[ 보기 ]

a. df['A']['B'] = value처럼 대괄호를 연속으로 사용하여 수정한다.

b. .loc[]를 사용하여 한 번의 단일 작업으로 위치를 지정해 수정한다.

c. 항상 원본 데이터 프레임을 여러 번 복사한 뒤 아무 방법으로나 수정한다.

d. 결측치가 있는 행을 모두 삭제한 뒤에만 수정이 가능하다.

A. .loc[]나 .iloc[]는 단일 작업으로 원본의 정확한 메모리 위치에 접근해 100% 안전하게 수정할 수 있습니다. a선지는 대괄호를 연속으로 사용하는 방식은 두 단계로 나뉘어 처리되어 임시 복사본이 생길 수 있어 위험하다.
